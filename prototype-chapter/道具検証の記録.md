# 道具検証の記録（捨て試作・レーン1 手順1〜5）

実施日: 2026-07-26 / 実施環境: macOS arm64, Node v22.23.1, npm 10.9.8

このディレクトリは**捨て試作**である。`material/` の外に置き、教材の成果物として
一切参照しない（`10_教材制作フロー.md` §3 の規定）。目的は「工程が回るか」の実証であり、
ここに書いた章そのものは商品にしない。

## 結論

**G3（文体ゲート）と G5（実装↔教材の同期）は、既製品の組み合わせで実際に動いた。**
自作コードは0行。設定ファイル2本（`.textlintrc.json` と `prh/sns-app.yml`）だけで成立した。

## 1. 導入した道具と実測結果

| 道具 | 版 | 導入経路 | 結果 |
|---|---|---|---|
| textlint | 15.7.1 | npm | 動作 |
| textlint-rule-preset-ja-technical-writing | — | npm | 動作（です・ます混在を実際に検出した） |
| textlint-rule-prh | — | npm | 動作 |
| embedmd | 1.0.0 | **Go 1.26.5 が必要**（`brew install go` → `go install`） | 動作 |

### ★ 計画になかった前提: embedmd は Go 製で、この機械に Go が入っていなかった

`brew install go`（約1分）＋ `go install github.com/campoy/embedmd@latest` で解決した。
**学習者には要求しない**（embedmd は制作側の道具であり、教材の手順には出てこない）。
ただし制作環境を別マシンへ移すときは Go の導入が要ることを記録しておく。

## 2. G3 文体ゲートの検証

### 検知テスト（禁止パターンを埋め込んだ章）

**9パターン中 9件を検出（100%）。**

| 種別 | 埋め込んだ文 | 検出 |
|---|---|---|
| AI構文 | それでは、型を作っていきましょう | ✓ |
| 誇大表現 | たった一行で投稿が作れます | ✓ |
| 誇大表現 | 誰でも簡単に理解できます | ✓ |
| 不採用スタック | Capacitor | ✓ |
| 不採用スタック | NestJS | ✓ |
| 不採用スタック | bcrypt | ✓ |
| 不採用スタック | Xcode | ✓ |
| 関西弁 | まだ動いとらん場合は | ✓ |
| AI構文 | いかがでしたでしょうか | ✓ |

### 初回は 6/9 しか検出できなかった（記録として残す）

最初に書いた正規表現が狭すぎた。実測しなければ気づかなかった取りこぼし:

| 取りこぼし | 原因 | 修正 |
|---|---|---|
| 「動いとらん」 | `しとらん` と書いており、活用の頭を固定していた | `とらん` へ緩めた |
| 「作っていきましょう」 | `していきましょう` と書いていた | `(?:して)?いきましょう` へ |
| 「誰でも簡単に」 | 直前の誇大表現ルールが同じ行を先に食っていた | 行を分けて再テストし、独立に発火することを確認 |

**教訓**: 禁止パターンの辞書は、書いた時点では動く気になるが、実際に食わせるまで
穴が見えない。**辞書を足すたびに検知テストを回す**を運用に入れる。

### 誤検知テスト（正常な章）

正常な章（`chapter.md`）で **発火ゼロ・exit 0**。

## 3. G5 引用ゲートの検証

### 日本語コメント入り TypeScript を正規表現の範囲で引用できるか → できた

```
[embedmd]:# (listings/node-npm-setup/index.ts ts /^type Post/ /^};/)
```

「`type Post` の行から `};` の行まで」を引用できた。日本語コメント（`// 本文（最大280文字）`）も
そのまま入る。**章の途中の書きかけコードを引ける**という前提が実物で確認できた。

### ズレを検出できるか → できた

`listings/` 側のコメントを「最大280文字」→「最大140文字」に変えたところ、
`embedmd -d` が差分を出して**非ゼロ終了**した。

### ★ 重要: `embedmd -d` の終了コードは 1 ではなく **2**

CI の合否判定を `exit 1` で書くと**ズレを見逃す**。判定は「非ゼロなら FAIL」と書くこと。
これは実際に動かさなければ分からなかった。

### 自動追従もできる

`embedmd -w` で教材側を実ファイルに合わせて書き換えられた。書き換え後は差分ゼロ。

## 4. まだ検証していないこと（正直に記す）

| 項目 | なぜ未検証か |
|---|---|
| **G6 実走検証（`exec` モード）** | **ここだけが未検証のまま残っている。** 読み比べ（`read` モード）は §11 で通したが、教材の手順を実際に走らせる工程は、この機械の共有フックが `npm install` を拒否するため一度も完走していない。詳細は §12 |
| ~~**面白さの計測**~~ | **決着（2026-07-27）。絶対評価は機能しないと分かり、比較評価へ作り替えた。結果は §8 と §11** |
| ~~**A7 章末スナップショット**~~ | **決着（2026-07-27）。生成スクリプトを作り、先の章のコードが混ざらないことも確認した。結果は §10 と §13 末尾** |
| ~~**1章あたりの工数**~~ | **決着（2026-07-27）。2章目を1本通しで作り約28分。結果は §13** |
| ~~**Expo の表示・web 出力**~~ | **決着（2026-07-27）。結果は §6** |
| ~~**章IDの再選定**~~ | **決着（2026-07-27）**。`node-npm-setup` は構成振り直し前の題材だったため、A0 の1章目を **`expo-first-screen`（Expo プロジェクトを作って最初の画面を出す）** に選び直した。`node-npm-setup` は文体ゲート検証用の素材として残す |

## 5. 10 / 14 への反映（証拠が出たので反映してよい項目）

- G3 を「textlint + preset-ja-technical-writing + prh（自作は YAML 1本）」に確定してよい
- G5 を「embedmd による引用」に確定してよい。**合否は `embedmd -d` の非ゼロ終了**
- G0 の条件を「edu-creator 改修完了」から「道具の導入完了」に差し替えてよい
  （導入が実際に完了したため、B13 の実装保留との循環が解けた）

## 6. Expo の表示と web 出力（2026-07-27 実測）

局長の指示（2026-07-27「実機はいま使っているので使わないで。emulator とかにして」）に従い、
**物理端末は一切使っていない**。使ったのは **iOS シミュレータ（iPhone 16 Pro / iOS 26.1）** と
**ヘッドレスの Chrome**。

| 検証 | 手段 | 結果 |
|---|---|---|
| ネイティブ表示 | iOS シミュレータ + Expo Go（SDK 57.0.0） | **表示された**。`App.tsx` の「はじめての画面」「ここから SNS を作っていきます」が出た |
| web 出力 | `expo export --platform web` → 静的配信 → ヘッドレス Chrome で撮影 | **同じ文字が web でも出た**。バンドルは 332KB / 179 modules |
| G3 文体ゲート | `npx textlint chapter-expo-first-screen.md` | 初回 **3件の指摘**（一文が長い / 弱い表現「かも」/ 助詞「が」の重複）→ 修正して **exit 0** |
| G5 引用ゲート | `~/go/bin/embedmd -d chapter-expo-first-screen.md` | 初回 **exit 2**（正規表現 `^});` の `)` を逃がしていなかった）→ `^}\);` に直して **exit 0** |

### ★ 計画の確信度が動いた項目

| 計画の項目 | 前 | 後 | 根拠 |
|---|---|---|---|
| 1. Expo + Supabase への振り直し（表示側） | 95% | **100%（表示の部分のみ）** | 同一の `App.tsx` がシミュレータと web の両方で描画されたのを画像で観測。Supabase 側は未検証なので、この 100% は**クライアントの表示だけ**に掛かる |
| 5. パートD（Webでも動かす）の分量 | 70% | **75%** | 「同じコードが web でも出る」は観測できた。ただし**部品を2つ足す手順が要る**ことが判明（F7）。章数そのものはまだ実測していない |

### ★ 教材へ効く発見（F7）

`expo export --platform web` は、`react-dom` と `react-native-web` が入っていないと止まる。
**「Expo は web に出せる」は本当だが、部品は最初から入っていない。**
パートDの1章目にこの手順を書かないと、読者全員が同じ所で止まる。

### 工数はこの時点では出せなかった

この章は道具の検証を兼ねており、純粋な執筆時間を切り出せていない。
**後に2章目で切り出して実測した（§13、約28分）。この節の「出せない」は §13 で解消済み。**

## 7. 面白さの計測（G6 の読み手設問・2026-07-27 実測）

### 隔離の実際

`chapter.md` だけを**リポジトリの外**（`/private/tmp` 配下）へ置き、そこを作業場所として
**別ファミリーのAI（Codex）を3体、独立に**走らせた。設計書・開発ログ・`listings/` は渡していない。

**隔離の穴を正直に書く**: 3体はファイルシステム全体を読める状態にあり、
「chapter.md 以外を見るな」という指示で縛っただけである。物理的な遮断ではない。
また `~/.claude` の運用ルールは読み込まれる（教材の設計文脈ではないが、ゼロではない）。

### 結果

| 設問 | PASS 条件 | 3体の回答 | 判定 |
|---|---|---|---|
| Q2 何につながる章か | 実際につながる場所と一致 | 3体とも「SNS の画面の土台」「起動時に出る画面」 | **PASS 3/3** |
| Q3 次を読みたいか | 「はい」 | 3体とも「はい」。理由も3体とも「書き換えがすぐ反映されるところを見たい」 | **PASS 3/3** |
| Q4 対話を読んだか | 「読んだ」 | 3体とも「読んだ」。理由は「エラーが出たときの対応が分かりやすい」「なぜ他の作業を止めたらあかんのかが分かる」 | **PASS 3/3** |

### ★ 1件だけズレが出た（記録として残す）

Q1（次に何ができるようになると思ったか）で、1体が
「**変更が端末へすぐ反映されるようになる**」と答えた。これは**次の章の内容**であり、
この章の内容ではない。原因は章末の「次の章では〜」の一文を、この章の成果として読んだこと。

対策の候補: 章末の予告を「この章でできたこと」から視覚的に離す、または予告を書かない。
**まだ直していない**。1/3 なので閾値（多数決）は割っていないが、
同じ形の誤読が他の章でも起きるなら構造の問題になる。

## 8. ★ 面白さの設問そのものが機能していなかった（2026-07-27・重要）

§7 は PASS しか出ていない。**PASS しか出ない道具は、合否を判定できているとは言えない。**
そこで、**わざと退屈に書いた同じ内容の章**（対話なし・箇条書きの手順書・
「本章では〜を行う」調）を作り、同じ3体に読ませた。

### 結果: 退屈な章でも「次を読みたい」が2/3で「はい」だった

| 設問 | 本物の章 | わざと退屈にした章 | 判別できたか |
|---|---|---|---|
| Q2 何につながるか | PASS 3/3 | PASS 3/3（「土台につながる」） | **できていない** |
| Q3 次を読みたいか | はい 3/3 | **はい 2/3** | **できていない** |
| Q4 対話を読んだか | 読んだ 3/3 | 読み飛ばした 3/3 | 対話の**有無**しか見ていない |

**原因**: Q3 は「次の章への期待」を聞いている。どんなに退屈な章でも
章末に「次章では画面を作る」と書いてあれば「はい」が返る。
**この章が面白かったか**ではなく、**次の章が面白そうか**を測っていた。

計画の §3「★面白さの計測」は、この設問を局長の成功基準の判定根拠に据えていた。
**その判定根拠は、実測したら機能していなかった。**

### 解法: 絶対評価をやめて、2本並べて選ばせる

同じ3体に A（本物）と B（退屈版）を両方読ませ、「どちらを買うか」を必ず一方選ばせた。

| 設問 | 結果 |
|---|---|
| Q5 どちらを買うか | **A が 3/3**。理由も3体とも「自分の端末に画面が出る達成感」「対話で詰まり方まで分かる」 |
| Q6 A で退屈だった箇所 | **3体とも「無し」** |
| Q7 B で退屈だった箇所 | 3体とも**同じ範囲**を指した（「1.4 ディレクトリ構成」「1.5 用語説明」） |

**並べて選ばせると判別できる。しかも退屈な箇所を場所で指してくる。**
絶対評価（この章は面白いか）は当たり障りのない「はい」に流れるが、
比較評価（どちらを選ぶか）は一方を捨てさせるので流れない。

### 追試: 差が小さい2本でも判別できるか → できた。しかも本物が負けた

「対話あり vs 対話なし」は差が大きすぎる。そこで**対話は残したまま、
詰まりの記述（ディスク不足・ポート競合・エラーの読み方）だけを抜いた版**を作り、
本物と比べさせた。

| 回 | 比較 | 結果 |
|---|---|---|
| 1回目 | 本物 vs 詰まり記述を抜いた版 | **抜いた版が 3/3 で勝った**。3体とも退屈な箇所として「詰まったときの読み方」を指した |
| 2回目 | 重複を削った本物 vs 同じ抜いた版 | **本物が 2/3 で勝った**（合否条件を満たす） |

**1回目で分かったこと**: 3体が指したのは「**すでに出た2つのエラーを、章の最後でもう一度
表にして説明し直す**」箇所だった。同じ内容を2回読ませていた。
表を消して1行にまとめたところ、2回目で判定が反転した。

**この道具は、狙って直した1箇所に反応して合否が変わる。** 粗い判別だけでなく、
**どこを直せばよいかを場所で返し、直した結果を再測定できる**。

### 正直に残す限界

この設問が測っているのは **読む前の好み**であって、**やってみた後の役に立ち方**ではない。
1回目で「詰まりの記述は退屈」と3体が言ったが、
**実際にディスクが埋まって手が止まった読者が同じことを言うかは分からない**。
それは実走（§9）でしか測れない。よって詰まりの記述は**消さずに、
出てくる場所へ埋め込んだ**（章末の再掲だけをやめた）。

### 3回目: 削っても判定が動かなくなった地点

2回目で残った「ポート競合のやり取りが長い」を受けて、そのやり取りを3往復から1往復へ縮めた。

| 回 | 本物の得票 | 退屈と指された箇所 |
|---|---|---|
| 2回目 | 2/3 | ポート競合のやり取りが長い（1体） |
| 3回目（縮めた後） | **2/3（変わらず）** | 「ディスク容量とポートのエラー説明」そのもの（2体） |

**指摘の中身が「長さ」から「その話題があること」へ移った。**
ここから先を削ると、詰まりの記述そのものが消える。

**削るのをやめた理由**: 読み手は**読む前**に判断しており、
実際に手が止まった経験をまだしていない。詰まりの記述が要らないかどうかは、
実走（§9）で「ディスクが埋まって止まった読者が同じことを言うか」を見るまで決められない。
**好みの投票だけで教える内容を削らない。**

現在の状態: 合否条件（本物が3体中2体以上）を**満たしている**。
残る 1/3 の反対票は、上記の理由で未処置のまま置く。

### 10 §4 への改訂（証拠が出たので反映してよい）

- G6 の面白さ判定を、**絶対評価から比較評価へ差し替える**
- 比較対象は「同じ内容を、対話なし・手順書調で書いた版」を各章ごとに用意する。
  これは執筆時に短時間で作れる（本文から対話を抜いて箇条書きに直すだけ）
- 判定は「本物が3体中2体以上で選ばれること」。加えて
  **「本物で退屈と指された箇所」がそのまま書き直しの対象になる**（場所が返るため）
- 旧 Q3「次を読みたいか」は**単独では合否に使わない**。次章への期待を測っており、
  この章の面白さを測っていない

## 9. G4 別ファミリーレビュー（2026-07-27 実測）

別ファミリー（Codex）に章を読ませ、観点3つ（技術正確性 / 完成品へのつながり / 文体逸脱）で
採点させた。退屈度は §8 の比較評価へ移したのでレビュー観点から外した。

| 観点 | 判定 | 内容 |
|---|---|---|
| 完成品へのつながり | **PASS** | 「この画面が少しずつ SNS になっていきます」等で出発点だと分かる |
| 文体逸脱 | **PASS** | 関西弁・誇大表現・AI定型句・不採用スタック名なし（G3 と一致） |
| 技術正確性 | **FAIL（7件）** | 下記 |

### ★ 最も重い指摘: 引用に `import` が入っていなかった

教材が `App.tsx` から引用していた範囲は `export default function App` からだった。
**`import` の2行が教材に一度も出てこない。** その通りに書き写した読者は、
`View` も `Text` も見つからずに失敗する。

**これは embedmd の部分引用そのものが持つ危険**である。
G5 は「教材と実ファイルがズレない」ことを保証するが、
**実ファイルの必要な部分を引用し損ねていること**は検出しない。
引用範囲が正しいかは、G5 では分からない。

対処: 引用範囲を `/^import/` からに変え、`import` が何なのかを1行で説明した。

### 処置した指摘

| 指摘 | 処置 |
|---|---|
| `import` が出てこない | 引用範囲を先頭からに変更 |
| Node.js / npm を暗黙に前提 | 「始める前に必要なもの」節を追加し、確認コマンドを載せた |
| 300MB を固定値として断定 | 「この本を書いた時点では」「目安」と明記 |
| QR の条件（Expo Go の導入・同一 Wi-Fi・`--tunnel`）が無い | 条件2つと `--tunnel` を追記 |
| 「止めると、そっちが壊れます」は断定が誤り | 「何なのか分からないうちは止めない」へ変更 |

### 未処置（判断が要る）

| 指摘 | なぜ未処置か |
|---|---|
| `create-expo-app@latest` は SDK 版を固定していない | **教材全体の方針**に関わる。版を固定すると古くなり、固定しないと再現できない。決定バックログ B16 として起票 |
| 「クリーンマシンでは `npx` 自体が存在しない」 | 前の章（Node.js 導入）の担当範囲。章分割が凍結してから確認する |

再走: G3 exit 0 / G5 exit 0（処置後）。

## 10. A7 章末スナップショット生成（2026-07-27 実装・実測）

`tools/make-snapshot.sh <章ID> [--verify]` を書いた。**66行**。自作2種のうちの1つ。

やること: `listings/<章ID>/` から、再取得できるもの（`node_modules` / `.expo` / `dist` 等）を
除いて `snapshots/<章ID>/` を作り、全ファイルの SHA-256 目録（`SNAPSHOT.txt`）を付ける。

### 走らせて見つかった欠陥（設計時には見えていなかった）

**1回目の生成物に `AGENTS.md` / `CLAUDE.md` / `.claude/settings.json` が入っていた。**
`create-expo-app` のテンプレートが AI 向けの指示ファイルを作るためで、
そのまま配ると**読者へ制作側の指示が渡る**。除外に追加した（17ファイル → 14ファイル）。

これは「先の章のコードが混入しないこと」（16 A7 の要件）とは別の混入経路である。
**要件が想定していなかった種類の混入**が、実際に走らせたら1回目で出た。

### `--verify` を付けたときの追加検証

スナップショットを**リポジトリの外の空のディレクトリ**へ複製し、`npm ci` が通るかを見る。
通れば「読者の手元でも成立する」と言える。**実測で通った。**

`package-lock.json` が無い場合は生成を止める（exit 2）。無いと読者の手元と教材で
入るものが変わるため。これは B16（SDK 版の固定）と同じ問題の、依存関係側の対処。

### まだやっていないこと

**「先の章のコードが混入しないこと」の確認**。章が1本しか無いので比較対象が無い。
2章目を書いた時点で、1章目のスナップショットに2章目のコードが入らないことを見る。

## 11. G6 実走ランナー（2026-07-27 実装・実測）

`tools/g6-run.sh <章ID> read|exec` を書いた。自作2種のうちの2つ目。これで道具4種が揃った。

隔離は `mktemp` で作った**リポジトリ外の空ディレクトリ**へ材料を出してから実行することで担保する。
`sns-app` 配下で走らせると CLAUDE.md と SessionStart hook が設計文脈を注入して隔離が破れる。

| モード | 渡すもの | 合否 |
|---|---|---|
| `read` | 本文と比較用（対話を抜いた版）の2本 | 本物が過半数で選ばれること |
| `exec` | 本文と、前章の章末スナップショット（`G6_PREV_SNAPSHOT`） | **全実行体が到達 かつ 教材外の知識で補った箇所が無いこと** |

`exec` の合否を「到達できたか」だけにしていない。進めたから PASS にすると、
実行体が自分の知識で埋めた穴が見えなくなる（16 A5）。

### 走らせて見つかった、自分の書いたコードの欠陥3件

| # | 欠陥 | どう出たか |
|---|---|---|
| 1 | `$N（` と書いた行で「未割り当ての変数」で落ちた | **全角の「（」が変数名の切れ目にならない**。`${N}` に直した。同じ書き方が他に無いか grep で確認済み |
| 2 | `exec` モードに合否判定が無く、到達できていないのに PASS を返した | 到達と補完なしの2条件を判定に追加。再走で exit 2 を返すことを確認 |
| 3 | 回答を取り出す `sed` が空を返した | **BSD sed は `\|` の選択肢を解さない**。`sed -nE` に直した |

3件とも、書いた時点では動くつもりでいた。**走らせるまで見えなかった。**

### `read` モードの実測

3体とも本物を選び **3/3 で PASS（exit 0）**。

**ただし票のばらつきがある。** 同じ2本で複数回まわしたところ 2/3 と 3/3 が両方出た。
3体で過半数（2票）という閾値は**ばらつきの幅とほぼ同じ**であり、
2/3 の結果は「勝った」と言い切れる余裕が無い。
体数を増やすか、判定を得票数そのもので見るかは未決。**未処置。**

### ★★ 較正テストで、判定が中身ではなく置き場所で決まっていたことが判明した

「ばらつきが閾値と同じ幅」を潰すために、**中身が完全に同じ2本**を比べさせた。
本来なら結果は五分五分になるはずである。

**3体とも A を選んだ。しかも2体が理由に「内容が同じなので、先に読んだ A を選びます」と書いた。**

原因は、**常に本物を A に置いていた**こと。実行体は先に読んだほうを選ぶ癖があり、
判定は中身ではなく置き場所で決まっていた。**§8 以降の PASS はすべてこの偏りを含んでいる。**

#### 直したこと

1. **本物の置き場所を実行体ごとに入れ替える**。奇数番は A、偶数番は B に置き、
   どちらに置いたかを記録して集計する
2. **体数を偶数にする**（既定4体）。奇数だと置き場所の数が釣り合わず、
   偏りだけで本物が過半数を取れる（実測: 中身が同じ2本で 2/3 が出て**合格になってしまった**）

#### 直した後の実測

| 比べたもの | 本物の得票 | 判定 |
|---|---|---|
| 中身が同じ2本（較正） | **2 / 4** | **FAIL**（正しい。偏りだけでは通らなくなった） |
| 本物 vs 対話を抜いた版 | 3 / 4 → 再確認 4 / 4 | **PASS** |

較正テストが FAIL を返すようになって初めて、この道具の PASS に意味が出た。

#### 長さで選んでいないかも確かめた

「先に読んだほう」以外の癖として、**長いほうを選ぶ**可能性が残る。
そこで対話を抜いた版に補足節を足して**本物より長く**し（6,574 バイト対 5,351 バイト）、
同じ比較をかけた。

| 比べたもの | 本物の長さ | 本物の得票 | 判定 |
|---|---|---|---|
| 本物 vs 対話を抜いた版（本物のほうが長い） | 5,351 / 2,628 | 4 / 4 | PASS |
| 本物 vs 補足を足して長くした版（**本物のほうが短い**） | 5,351 / 6,574 | **4 / 4** | PASS |

**本物が短いほうに回っても結果が変わらなかった。** 長さでは選んでいない。

較正で確かめたこと: 置き場所では選ばない（入れ替えて集計）、長さでは選ばない（上表）、
中身が同じなら通らない（2/4 で FAIL）。

### `exec` モードは基盤に阻まれて完走していない

実行体は `create-expo-app` まで進み、**`npm install` で止まった**。
止めたのは教材ではなく、**この機械に入っている PreToolUse hook**
（「このコマンドは Codex へ委譲せよ」と拒否する）。
実行体自体が Codex なので、この規則がここでは空回りしている。

- ランナーの配管（材料の隔離・起動・報告の回収・合否・exit 2）は**通しで動いた**
- **教材そのものの実走検証は、まだできていない**。PASS とは言えない
- 直すには機械側の hook 設定を変える必要があり、共有設定なので今は触らない

## 12. 実走検証（G6 `exec`）が残した唯一の穴

この節が、捨て試作を通して**ただ一つ PASS と言えないまま残った項目**である。

**実走（設問4「実際にできたか」・設問5「手が止まった箇所」）**。
`npx create-expo-app` から実際に走らせる工程で、ディスクの空きが要る。
今回の作業中に空き容量が3回枯渇しており（dev-log F4 / F4-2）、
他の作業を巻き込む危険があるため実施していない。**PASS とは言えない。**

塞ぐには次の2つが要る。どちらも作業そのものは軽いが、片方は共有設定に触る。

1. この機械の PreToolUse hook に、G6 実行体が使う一時ディレクトリ配下だけの例外を足す。
   共有設定なので、変更内容を先に見せて承認を得てから入れる。
2. 実行前にディスクの空きを確保する（3回枯渇している以上、事前確認を手順に入れる）。

**この穴が Phase 0 の承認を止めるわけではない**（14 の Phase 0 完了条件に G6 は入っていない）。
止まるのは Phase 2 の完了条件「捨て試作1章が G3〜G6 を実際に通過していること」である。

## 13. ★ 1章あたりの工数（2026-07-27 実測。計画の確信度0%だった項目）

道具が揃った状態で **2章目（`live-reload`）を1本通しで作り、時間を計った**。

| 区間 | 内容 |
|---|---|
| 計測方法 | 着手時刻と完了時刻の差（実時間）。開始 06:59:21 |
| **実測値** | **1,684 秒 = 約 28 分** |
| 含むもの | listings の実装 / シミュレータでの実物確認（変更前と変更後の画面写真）/ 本文執筆 / G3 / G5 / 比較用の版の生成 / G6 read（4体1周・PASS 4/4）/ A7 スナップショット / 開発ログ記録 |
| 含まないもの | 道具の導入（1章目に一度だけ）/ G4 別ファミリーレビュー（2章目では未実施） |

### この28分の内訳で目立つもの

うち **約8分はシミュレータの詰まり**（dev-log F8。別プロジェクトのアプリと入口が衝突し、
再起動で復旧）に消えている。**素の作業は約20分**。

### 1章目との差

| | 1章目 | 2章目 |
|---|---|---|
| 詰まりの件数 | 7件（F1〜F7） | 1件（F8） |
| 道具の導入 | あり | なし |

**1章目の時間を「1章あたり」として掛け算してはいけない**（計画 §8 の失敗シナリオ2そのもの）。

### 全体の桁（この1点から出せる範囲で）

章数はまだ確定していない（15 §3 は空欄のまま）。旧構成の 47〜58章を仮に当てると:

| 前提 | 見積もり |
|---|---|
| 28分 × 47章 | 約 22 時間 |
| 28分 × 58章 | 約 27 時間 |

**この数字を信じてはいけない理由**:

- 2章目は**前章の続きに1行足すだけ**の、パートA0で最も軽い部類の章である
- 後半の章（Supabase の RLS、認証、カメラ、Realtime）は実装量が桁で違う
- **G4 別ファミリーレビューと、その指摘の処置が入っていない**。
  1章目では G4 が7件返し、その処置に相応の時間がかかった
- G6 の実走（`exec`）が一度も完走していない。完走すればさらに乗る

**したがって 22〜27 時間は下限であり、上限ではない。**
実際の見積もりに使うなら、重い章を1本（Supabase の RLS を書く章など）計るまで待つべきである。

### A7 の残り課題も、ここで潰れた

§10 で「章が1本しか無いので、先の章のコードが混入しないことを確認できない」と書いた件。
2章目ができたので照合した。

- `snapshots/expo-first-screen/App.tsx` に `styles.note`（2章目で足した行）が **0件**
- `snapshots/live-reload/App.tsx` に **1件**

**先の章のコードは1章目のスナップショットに入っていない。**
