# D14 — 検査スクリプト28本と自己テスト17本の実走結果

作成: 2026-08-06
対象: `scripts/curriculum-qa/`（前作 task-app から無変更で搬入した28本＋テスト17本）
出どころ: `material/decisions/制作前レディネス監査.md` D14（218-228行）
前提: `material/decisions/D1_教材の置き場と章IDの規約.md`（D1-1〜D1-8）

実行環境: Python 3.11.15 / Linux。リポジトリ根は `/workspace/sns-app`。
本書の出力はすべて実際に走らせて得たもので、docstring からの推定は含まない。

---

## 0. 実走に使った入力

本作にはまだ教材本文が1行も無い。捨て試作の2章を **D1-2 の命名（`curriculum/<章ID>.md`）** で
置いた一時ディレクトリを作り、これを入力にした。

```
$ cp prototype-chapter/chapter-expo-first-screen.md <tmp>/curriculum/expo-first-screen.md
$ cp prototype-chapter/chapter-live-reload.md       <tmp>/curriculum/live-reload.md
```

前作の命名（`dayNN_*.md`）との差だけを見たい検査については、同じ内容を `day01_x.md` に
置き換えた入力も併せて流した。

---

## 1. pytest の実行環境（監査 D14-4）

### 1-1. 導入前

```
$ python3 -m pytest --version
/usr/local/bin/python3: No module named pytest
```

`requirements.txt` も `pyproject.toml` も無い。`.github/workflows/gates.yml` に Python の
ステップは無い（`actions/setup-node` と `actions/setup-go` のみ。gates.yml:36-46）。

### 1-2. 導入後 — **pytest では1件も走らない**

```
$ python3 -m pip install pytest      # pytest 9.1.1
$ cd scripts/curriculum-qa && python3 -m pytest -q
no tests collected in 0.03s
$ echo $?
5
```

理由は搬入した17本が **pytest 形式ではない**こと。17本すべてに `def test_*` も
`unittest.TestCase` も存在しない。

```
$ grep -lE "^def test|^    def test|unittest" test_*.py
（0件）
```

17本は `CASES` というリストを持ち、`main()` が自前でループして `❌`/`✅` を印字し
`sys.exit(0|1)` する**単体スクリプト形式**である（例: `test_check_variants.py:68-80`）。
`check_quality.sh:229-247` の `SELF_TESTS` 配列も `python3 "$SCRIPT_DIR/$t.py"` として
1本ずつ直接呼んでおり、pytest を前提にしていない。

**pytest を入れても検知テストの実体は1件も走らない。** rc=5（`EXIT_NOTESTSCOLLECTED`）は
CI の書き方によっては緑にも赤にもなるが、いずれにせよ「17本を走らせた」ことにはならない。

### 1-3. 17本の直接実行 — 11本合格・6本失敗

`python3 test_xxx.py` を1本ずつ実行した生の結果。

| # | テスト | rc | 中身 |
|---|---|---|---|
| 1 | `test_markdown_scan.py` | 0 | `✅ markdown_scan の退行テスト OK` |
| 2 | `test_check_crossref.py` | 0 | `✅ 全 10 件 PASS` |
| 3 | `test_check_variants.py` | 0 | `✅ check_variants 自己テスト 16/16 合格` |
| 4 | `test_check_step_time.py` | 0 | `✅ check_step_time 自己テスト 15/15 合格` |
| 5 | `test_check_procedure_order.py` | 0 | `✅ check_procedure_order 自己テスト 8/8 合格` |
| 6 | `test_check_step_ref.py` | 0 | `✅ check_step_ref 自己テスト 14/14 合格` |
| 7 | `test_check_jsx_marker.py` | 0 | `✅ JSX の位置の目印 自己テスト 17/17 合格` |
| 8 | `test_check_ja_line_break.py` | 0 | `✅ 日本語の文の折り返し 自己テスト 26/26 合格` |
| 9 | `test_check_scaffold_alignment.py` | 0 | `✅ scaffold 整合性の目印 自己テスト 4/4 合格` |
| 10 | `test_check_why.py` | 0 | `✅ check_why 自己テスト 9/9 合格` |
| 11 | `test_filepath_marker.py` | 0 | `✅ 貼り先の目印 自己テスト 13/13 合格` |
| 12 | `test_check_anchor.py` | **1** | `❌ check_anchor 自己テスト 2/21 失敗` |
| 13 | `test_sale_package.py` | **1** | `FileNotFoundError: .../scripts/build-zip.sh` |
| 14 | `test_check_tag_balance.py` | **1** | 同上 |
| 15 | `test_check_false_success.py` | **1** | 同上 |
| 16 | `test_check_zip_reference.py` | **1** | 同上 |
| 17 | `test_check_unused_image.py` | **1** | `FileNotFoundError: .../.github/workflows/material-gate.yml` |

合格した11本の中で数えられているケース総数は 132 件（10+16+15+8+14+17+26+4+9+13）。
`test_markdown_scan.py` は件数を印字しない。

### 1-4. 失敗6本の内訳

**13〜16 の4本は同じ1個の欠落に落ちている**: `scripts/build-zip.sh`（前作の販売ZIP生成
スクリプト。搬入していない）。搬入対象を `scripts/curriculum-qa/` に限ったための欠落で、
検査ロジックの欠陥ではない。

**17 は参照先が `.github/workflows/material-gate.yml`**（前作のワークフロー名）。本作の
ワークフローは `gates.yml` で、名前が違う。`test_check_unused_image.py:120` が
`Path(__file__).resolve().parents[2] / ".github/workflows/material-gate.yml"` を直に読む。

**12 は本作のリポジトリ内容そのものに依存した失敗**で、他の5本と性質が違う。

```
$ python3 test_check_anchor.py
  ❌ 完成版に在るパスは通す: 期待 [] / 実際 [(2, 'src/app/page.tsx')]
  ❌ 在るパスに注記が付いていても通す: 期待 [] / 実際 [(2, 'src/app/page.tsx')]
❌ check_anchor 自己テスト 2/21 失敗
```

`check_anchor.py:95` の `if not (root / value).exists()` が、リポジトリ根に実在する
ファイルかどうかを見る。前作には `src/app/page.tsx` が在り、本作には無い。
**この検査は「教材が指す貼り先が正本に実在するか」を見る検査なので、正本のディレクトリ構成に
直結する。** D1-5 が正本を `app/ + supabase/` と決めた以上、`check_anchor.py:73,93` の
プレフィックス `("src/", "prisma/", "scripts/")` は本作では1件も当たらない（§3-2）。

### 1-5. 依存関係（実測）

17本の import はすべて標準ライブラリ（`sys` / `pathlib` / `tempfile` / `contextlib` /
`io` / `subprocess` / `json`）と同ディレクトリの検査本体のみ。**第三者パッケージは1つも
使っていない。requirements.txt も pyproject.toml も要らない。**

---

## 2. 依存グラフ（監査 D14-1）

### 2-1. import 依存

`markdown_scan.py` が唯一の葉（標準ライブラリのみ）。以下は矢印の先が依存先。

```
markdown_scan.py（依存なし）
 ├─ curriculum_blocks.py
 │   ├─ check_code_completeness.py
 │   ├─ check_no_skip.py
 │   ├─ check_scaffold_curriculum_alignment.py
 │   │   └─ sale_package.py ── (外部) scripts/build-zip.sh
 │   │        ├─ check_tag_balance.py
 │   │        │   └─ check_false_success.py
 │   │        └─ check_zip_reference.py
 │   └─ check_false_success.py（curriculum_blocks も直接 import）
 ├─ check_anchor.py / check_crossref.py / check_ja_line_break.py / check_jsx_marker.py
 ├─ check_procedure_order.py / check_step_ref.py / check_step_time.py / check_terms.py
 ├─ check_tone.py / check_unused_image.py / check_variants.py / check_why.py
 └─（依存なし）check_comprehension.py / check_step_length.py / check_tech_stack.py
                / check_visualization.py / gen_procedure_map.py
```

### 2-2. 監査が挙げた連鎖の実測値

監査 D14-1 は連鎖を「check_false_success:22 → check_tag_balance:47 → sale_package:29-30」と
書いているが、実走の traceback は別の行を通る。**import 時ではなく実行時に落ちる。**

```
$ python3 check_false_success.py <tmp>/curriculum/expo-first-screen.md
  File "check_false_success.py", line 107, in <module>
  File "check_false_success.py", line 94, in main
  File "check_false_success.py", line 53, in find_claims
  File "check_tag_balance.py", line 183, in find_unclosed
  File "sale_package.py", line 160, in scaffold_src_paths
  File "sale_package.py", line 129, in _scaffold_copies
  File "sale_package.py", line 94, in excluded_routers
  File "sale_package.py", line 66, in _build_zip_text
FileNotFoundError: [Errno 2] No such file or directory: '/workspace/sns-app/scripts/build-zip.sh'
```

`sale_package.py` は `@cache` を付けた関数の中で `build-zip.sh` を読む（`sale_package.py:56-66`）。
**import は通り、値が要求された瞬間に落ちる。** 結論（◎を先に配線しても△を作り直すまで
起動しない）は変わらないが、行番号と発火のタイミングは上記が正しい。

### 2-3. この遅延がもう1つ生む症状 — `check_zip_reference.py` は入力しだいで結果が変わる

同じスクリプトが、入力に当たりが無ければ緑を返し、当たった瞬間にクラッシュする。

```
$ python3 check_zip_reference.py <tmp>/curriculum/expo-first-screen.md
✅ 照合先 OK（1 ファイル）                       … rc=0

$ cat <tmp>/probe/zip-probe.md
リポジトリの `src/app/page.tsx` と見比べて確認してください。
$ python3 check_zip_reference.py <tmp>/probe/zip-probe.md
FileNotFoundError: ... /scripts/build-zip.sh   … rc=1
```

**違反が無いときだけ通る検査は、緑を根拠にできない。** 依存先が欠けている状態では
「違反ゼロ」と「依存が無くて判定していない」が同じ出力になる。

### 2-4. 外部ファイル依存（実測。リポジトリ根からの相対）

| スクリプト | 読む外部ファイル | 本作での有無 |
|---|---|---|
| `sale_package.py` | `scripts/build-zip.sh` | **無い** |
| `check_tag_balance.py` | 同上（`sale_package` 経由・遅延） | **無い** |
| `check_false_success.py` | 同上（`check_tag_balance` 経由・遅延） | **無い** |
| `check_zip_reference.py` | 同上（`sale_package` 経由・遅延） | **無い** |
| `check_scaffold_curriculum_alignment.py` | `scripts/scaffold-from-scratch.sh` / `package.json` / `material/30days-curriculum/day*.md` / `src/command/seed.ts` / `src/lib/rate-limit.ts` / `src/server/api/routers/auth.ts` / `src/middleware.ts` | **全部無い** |
| `check_anchor.py` | リポジトリ根の実ファイル（`src/` `prisma/` `scripts/` 配下） | **無い** |
| `gen_procedure_map.py` | `scripts/_server-routers/*.ts` / `scripts/_app-components/` / `scripts/_app-base/` / `scripts/_app-api-trpc/` / `src/` | **全部無い** |
| `check-sale-package.sh` | `task-app-curriculum-v1.1.zip` / `scripts/_*` 13ディレクトリ | **全部無い** |
| `check_unused_image.py` | 入力ディレクトリ配下の `screenshots/` | 本文と同時に作る |
| 上記以外の19本 | 無し（入力の .md だけを読む） | — |

実走の確認:

```
$ python3 check_scaffold_curriculum_alignment.py <file>
FileNotFoundError: ... /scripts/scaffold-from-scratch.sh          rc=1

$ python3 gen_procedure_map.py
[FATAL] router source dir not found: /workspace/sns-app/scripts/_server-routers   rc=2

$ bash check-sale-package.sh
販売用 ZIP が見つかりません: /workspace/sns-app/task-app-curriculum-v1.1.zip      rc=1
```

---

## 3. D1 の命名で実際に動くか（監査 D14-3）

### 3-1. ディレクトリを渡したとき

`<tmp>/curriculum/`（中身は `expo-first-screen.md` と `live-reload.md`）を渡した結果。

| スクリプト | rc | 出力 | 判定 |
|---|---|---|---|
| `check_anchor.py` | 0 | `✅ コードブロックの書き込み先 OK（2 ファイル）` | 走った（`*.md` glob） |
| `check_variants.py` | 0 | `✅ 同じ語の書き分けなし（2 ファイル）` | 走った（`*.md` glob） |
| `check_zip_reference.py` | 0 | `✅ 照合先 OK（2 ファイル）` | 走った（§2-3 の注意付き） |
| `check_ja_line_break.py` | 0 | `✅ …（コードブロック 2 個）` | 走った（`rglob("*.md")`） |
| `check_jsx_marker.py` | 0 | `✅ …（コードブロック 4 個）` | 走った（`rglob("*.md")`） |
| `check_why.py` | **2** | `❌ 対象ファイルがありません` | **dayNN glob で0件。fail closed** |
| `check_step_ref.py` | **2** | 同上 | 同上 |
| `check_step_time.py` | **2** | 同上 | 同上 |
| `check_tag_balance.py` | **2** | 同上 | 同上 |
| `check_false_success.py` | **2** | 同上 | 同上 |
| `check_procedure_order.py` | **2** | 同上 | 同上 |
| `check_crossref.py` | **2** | `❌ dayファイルが見つかりません` | 同上 |
| `check_terms.py` | **2** | `❌ 教材ファイルが見つかりません` | 同上 |
| `check_unused_image.py` | 2 | `❌ 画像がありません: …/screenshots` | 命名ではなく `screenshots/` 不在 |
| `check_code_completeness.py` | 1 | `IsADirectoryError` | ディレクトリを受け取れない（1ファイル専用） |
| `check_comprehension.py` | 1 | 同上 | 同上 |
| `check_no_skip.py` | 1 | 同上 | 同上 |
| `check_step_length.py` | 1 | 同上 | 同上 |
| `check_tech_stack.py` | 1 | 同上 | 同上 |
| `check_tone.py` | 1 | 同上 | 同上 |
| `check_visualization.py` | 1 | 同上 | 同上 |
| `check_scaffold_curriculum_alignment.py` | 1 | `FileNotFoundError`（引数を見ない） | 外部依存 |

**dayNN glob の8本は非ゼロで落ちる（fail closed）。** 命名を直すまで動かないが、
黙って緑にはならないので前作#10 の再生産経路ではない。

### 3-2. 1ファイルを渡したとき — **0件走査で緑になる6本**

`<tmp>/curriculum/expo-first-screen.md` を渡した結果のうち、rc=0 でありながら
**実際には何も検査していない**もの。

| スクリプト | rc | 出力 | 0件で緑になる理由 |
|---|---|---|---|
| `check_step_time.py` | **0** | `✅ 所要時間の合計 OK（0 ファイル）` | `day_number()` が `dayNN` 以外に `None` を返し、`check_step_time.py:88-89` が「表を持たない日」判定に入る前に対象から外す。**渡した1ファイルは検査もされず、欠落としても数えられない** |
| `check_no_skip.py` | **0** | `検出されたステップ数: 0` / `✅ 全0ステップが完全` | ステップが0本でも合格を印字する |
| `check_code_completeness.py` | **0** | `filepath:コメント付き: 0` / `⚠️ …PASS（警告あり）` | `filepath:` が1つも無くても警告どまり |
| `check_tech_stack.py` | **0** | `⚠️ shadcn/uiパターンが検出されませんでした` / `✅ …PASS` | 同上。本作に shadcn/ui は無いので**常に緑** |
| `check_comprehension.py` | **0** | `ℹ️ 確認ポイント: Step形式のセクションなし（チェックスキップ）` | Step 見出しが無いと丸ごとスキップ |
| `check_procedure_order.py` | **0** | `✅ 手続きを使う順番 OK（1 ファイル / 手続き 0 個）` | tRPC ルータの `filepath:` が無ければ0件。**Supabase 構成では常に0件＝常に緑** |
| `check_anchor.py` | **0** | `✅ コードブロックの書き込み先 OK（1 ファイル）` | `find_missing` の対象プレフィックスが `("src/","prisma/","scripts/")`（`check_anchor.py:73,93`）。D1-5 の `app/ + supabase/` は1件も当たらない |

**これは D1-8(3) が定めた共通規約「走査対象0件は FAIL、対象はあっても実際に検査した件数が
0なら FAIL」が要る、そのものの実物である。** 監査が指摘した `check_step_time.py` だけでなく、
少なくとも7本が同じ形を持っている。

### 3-3. `dayNN` 命名へ戻したときの `check_step_time.py`

同じ2本を `day01_x.md` / `day05_y.md` に改名して流した。

```
$ python3 check_step_time.py <tmp>/dayprobe/day01_x.md
✅ 所要時間の合計 OK（0 ファイル）                                  rc=0

$ python3 check_step_time.py <tmp>/dayprobe/day05_y.md
❌ 所要時間の記載が見つからないファイル 1 件
  day05_y.md: ステップ表の所要時間の行と**合計時間** の行 がありません   rc=1
```

`NO_TABLE_DAYS`（day01〜04）に入る番号は**前作の命名でも同じ緑を返す**。番号が外れれば
落ちる。つまり前作は「暗黙免除は4日分だけ」という形で被害を4章に閉じていた。

**D1 命名にすると `day_number()` が全ファイルに `None` を返すので、この免除が全章へ広がる。**
（`check_step_time.py:30-31, 88-89`）。命名を前作へ戻せば直る、という話ではなく、
免除の判定を番号から外す必要がある — D1-8 が「`day(\d{2})` で順序を得ている検査は
`chapter_table.py` から並び順を得る形へ替える」と書いた対象がここである。

### 3-4. `check_quality.sh` は D1 命名を明示的に拒否する

```
$ bash scripts/curriculum-qa/check_quality.sh <tmp>/curriculum
❌ エラー: 命名規則外の md ファイルを検出しました（品質ゲートの対象外になるため許可できません）:
<tmp>/curriculum/expo-first-screen.md
<tmp>/curriculum/live-reload.md
   day ファイルは day[0-9][0-9]_*.md 形式に、付録は appendix_*.md 形式にリネームしてください
rc=1
```

`check_quality.sh:26-38` のガードによる。**D1 の章IDを使う限り、統合スクリプトは
ディレクトリ入力を1件も通さない。**

さらに1ファイル入力にしても、corpus 側は黙って飛ぶ。

```
$ bash scripts/curriculum-qa/check_quality.sh <tmp>/curriculum/expo-first-screen.md
…
⏭️  corpus 全体チェックは対象外（day ファイルがありません）: <tmp>/curriculum
❌ SOME CHECKS FAIL     rc=1
```

`check_quality.sh:266-271` の `compgen -G "$root/day[0-9][0-9]_*.md"` が偽になると
**corpus 検査14本を丸ごとスキップし、スキップしたこと自体は合否に影響しない。**
この実行が赤いのは自己テスト6本の失敗（§1-3）と単体検査の失敗によるもので、
corpus 14本は1本も走っていない。

### 3-5. `curriculum_blocks.day_number()` は D1 命名で全ファイルを 0 にする

```python
# curriculum_blocks.py:84-85
def day_number(name: str) -> int:
    m = re.match(r"day(\d{2})", name)
    return int(m.group(1)) if m else 0
```

`check_false_success.py:64-70` と `curriculum_blocks.py:146` の並べ替えがこの値を使う。
章IDのファイル名では**全章が同じ 0 に潰れ、章の前後関係が消える。**例外も警告も出ない。

ここはコードの読み取りであり、実走で確かめていない — 依存先の `sale_package.py` が
`build-zip.sh` の欠落で落ちるため、`check_false_success.py` を D1 命名の入力で
最後まで走らせられない（§2-2）。**`sale_package.py` を作り直したあとに、章順の取り違えが
実際に起きるかを陽性サンプルで測ること。** D1-8 が「`day(\d{2})` で順序を得ている検査は `chapter_table.py` から
並び順を得る形へ替える」と書いた対象に、この関数も入る。

---

## 4. 分類表に無かった3本（監査 D14-2）

| スクリプト | 実走結果 | 分類 |
|---|---|---|
| `markdown_scan.py` | `python3 markdown_scan.py` rc=0（ライブラリなので何も印字しない）。自己テスト `test_markdown_scan.py` rc=0。標準ライブラリのみ。20本が import する土台 | **◎** |
| `curriculum_blocks.py` | rc=0。`markdown_scan` のみに依存。ただし `day_number()` が dayNN 前提で、非該当は 0 を返す（§3-5） | **○**（`day_number` を章分割表の並び順へ差し替え） |
| `gen_procedure_map.py` | `[FATAL] router source dir not found: scripts/_server-routers` rc=2。tRPC ルータ・`_app-*` 配布ディレクトリ・`src/` が前提。出力先も `material/30days-curriculum/_meta/` | **×（本作では使わない）** |

`×` は新しい区分である。`gen_procedure_map.py` は検査ではなく **tRPC の手続きと day の
対応表を生成する道具**で、Supabase SDK 構成の本作には対応物が無い。作り直しではなく
不使用として扱う（B31 の分類語彙「そのまま使う／手直し／作り直し／不要」の「不要」にあたる）。

これで 25本 + 3本 = **28本すべてが分類表に載る。**

---

## 5. 実走を踏まえた分類の変更

§1〜§4 の結果から、`task-app資産棚卸し.md` §4 の分類を次のように変える
（変更後の表は同ファイル §4 に反映済み）。

| スクリプト | 旧 | 新 | 変更の根拠 |
|---|---|---|---|
| `check_anchor.py` | ◎ | ○ | プレフィックスが `src/ prisma/ scripts/`。自己テストが本作で 2/21 失敗（§1-4） |
| `check_false_success.py` | ◎ | ○ | `sale_package`（△）へ遅延依存。`day_number` で章順が潰れる（§2-2, §3-5） |
| `check_step_time.py` | ◎ | ○ | 0件走査で緑（§3-2）。番号ベースの暗黙免除が D1 命名で全章へ広がる（§3-3） |
| `check_step_ref.py` | ◎ | ○ | dayNN glob（§3-1） |
| `check_why.py` | ◎ | ○ | 同上 |
| `check_crossref.py` | ◎ | ○ | dayNN/`00*`/`appendix_*` glob |
| `check_terms.py` | ◎ | ○ | 同上 |
| `check_comprehension.py` | ◎ | ○ | Step 無しで丸ごとスキップ。用語リストが Next.js/tRPC/shadcn 寄り（`check_comprehension.py:20-21`） |
| `check_code_completeness.py` | ◎ | ○ | `filepath:` ゼロでも警告どまりで PASS |
| `check_no_skip.py` | ◎ | ○ | ステップ0本で合格 |
| `check_unused_image.py` | ◎ | ○ | 本体は命名非依存で動くが、自己テストが `.github/workflows/material-gate.yml` を読む |
| `check_variants.py` | ◎ | ◎ | 変更なし。`*.md` glob で D1 命名のまま走り、自己テスト 16/16 |
| `check_tone.py` | ◎ | ◎ | 変更なし。実入力で1件検出して rc=1 を返した |
| `check_step_length.py` | ◎ | ◎ | 変更なし。`✅ 全6個のコードブロックが25行以下` |
| `check_visualization.py` | ◎ | ◎ | 変更なし。実入力で不足2件を検出して rc=1 |
| `markdown_scan.py` | 表外 | ◎ | §4 |
| `curriculum_blocks.py` | 表外 | ○ | §4 |
| `gen_procedure_map.py` | 表外 | × | §4 |

◎ は 15本 → **5本**（`check_variants` / `check_tone` / `check_step_length` /
`check_visualization` / `markdown_scan`）。**実走で確かめた ◎ はこの5本しか残らない。**

---

## 6. まだ決められないこと

- **`check_anchor.py` の許可プレフィックスを何にするか。** D1-5 は正本を `app/ + supabase/` と
  決めているが、Expo プロジェクトを1本も作っていないため、`components/` や `lib/` を根に置くのか
  `app/` の下に置くのかが現物で確かめられない。誤ったプレフィックスを入れると §3-2 と
  同じ「常に緑」を作る。**Expo プロジェクトの scaffold を1本作って実測してから決める。**
- **`check_quality.sh` のガードを何に置き換えるか。** D1-6 の `material/17_章分割表.md` が
  まだ存在しないため、「許可される .md の集合」を章分割表から引く形に書き換えられない。
  D1-8(2) の `scripts/check-gate-wiring.sh` と同じ読み取り口（`chapter_table.py`）を使う想定だが、
  そのファイルも未作成である。
- **17本の検知テストを CI のどのジョブに、どの粒度で並べるか。** D13（G3 の残り4検査の起票）と
  同時に決める必要がある。本書で確定できるのは「pytest は不要」「第三者依存はゼロ」
  「17本は `python3 <file>` で個別に呼ぶ」の3点まで。

---

## 7. 本書で確認できた、監査の記述とのズレ

- 監査 D14-1 の連鎖「`check_false_success:22 → check_tag_balance:47 → sale_package:29-30`」は
  **import 行の番号**である。実走の traceback は `check_false_success:53 →
  check_tag_balance:183 → sale_package:160→129→94→66` を通り、**import 時ではなく
  値が要求された時点で落ちる**（§2-2）。結論は変わらないが、`sale_package.py` を
  作り直すまで「import は通るが実行時に落ちる」ため、import が通ることを動作確認と
  読み違えないようにする。
- 監査 D14-4 は「`python3 -m pytest` は `No module named pytest`」と書いており事実だが、
  **pytest を入れても解決しない**（§1-2）。17本は pytest 形式ではない。
