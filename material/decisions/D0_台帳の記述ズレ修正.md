# D0 / D0b — 台帳・提出物の記述ズレと現物の後始末（2026-08-06）

対象: `制作前レディネス監査.md` の D0（記述ズレ8件＋承認パッケージの差分）と D0b（現物3件）。

方法: 監査の記述を根拠として採らず、**1件ずつ現物を開いて再確認した**。
その結果、監査の記述そのものが現物とズレていた箇所が2件あった（§4 に記録）。

共有文書（10 / 12 / 13 / 16 / PROGRESS）への変更は本書では書き換えず、
`shared_edits` として統合役へ渡す。本書が直接触ったのは現物のみ（§3）。

---

## 0. 結果の一覧

| # | 対象 | 現物確認 | 処置 |
|---|---|---|---|
| 1 | 16:362 引き継ぎ表#4 の完了条件 | 成立 | shared_edits（`npm run gate` → `npm run g6 <章ID> exec`） |
| 2 | 10:307,311 G6 の合否条件 | 成立 | shared_edits（3体中2体 → 偶数体・既定4体・過半数） |
| 3 | 10:83-85 G0 の進捗行 | 成立 | shared_edits（未着手 → 実装・実測済み、ただし exec 未完走） |
| 4 | 10:208-211 G4 の観点数 | 成立 | shared_edits（合否観点を3つに、退屈度は G6 へ） |
| 5 | 13:55 対象文書番号 | 成立（ただし監査の行数が誤り） | shared_edits（00〜15 → 00〜16） |
| 6 | PROGRESS:113 B群母数 | 成立 | shared_edits（全30件 → 全34件・29件未決定） |
| 7 | 16:283 C群の表ヘッダ | 成立 | shared_edits（「いつ」列を復活。7箇所） |
| 8 | 16:160,166,168,360,361 期限用語 | 成立（変種は5つあった） | shared_edits（「1章目の執筆開始前」へ統一） |
| 9 | 12:111-112 の断定形 | 成立 | shared_edits（未実装であることを書く） |
| 10 | 10:31 main_pm.py の行番号 | 成立 | shared_edits（L233 → :52 と :186-189） |
| 11 | 承認パッケージの差分 | 成立（10 が +28 行、12 が +21 行） | §2 に差分1枚を用意。提出は統合役 |
| D0b-1 | 追跡下の symlink | 成立（1件のみ） | **実行済み**（§3.1） |
| D0b-2 | 対応物の無い workflow | 一部不成立（§4.2） | 判断は §3.2。削除操作は環境が拒否したため未実行 |
| D0b-3 | GENERICIZATION_PLAN.md の所在 | 成立（どこにも無い） | §3.3。B13 は局長判断キューへ |

---

## 1. D0 の8件（現物照合の記録）

### 1-1. 16:362 引き継ぎ表 #4 の完了条件

現物: 完了条件は「共有フックが `npm install` を拒否する問題を解消して `npm run gate` を通す」。

`prototype-chapter/package.json` の `scripts` は

```
:7  "gate:style": "tools/g3-style.sh",
:8  "gate:quote": "tools/g5-quote.sh",
:9  "gate": "npm run gate:style && npm run gate:quote",
:10 "snapshot": "tools/make-snapshot.sh",
:11 "g6": "tools/g6-run.sh"
```

`gate` は G3 文体チェックと G5 引用ゲートの2本で、G6 実走ランナーを一度も呼ばない。
`tools/g6-run.sh`:27 の usage は `<章ID> read|exec` で、引数2つを要求する。
`道具検証の記録.md` §12 が「ただ一つ PASS と言えないまま残った項目」と書くのは
`exec`（実走）であって `read`（読み比べ）ではない。よって完了条件は
`npm run g6 <章ID> exec` の完走でなければ、記述どおりに作業しても G6 は走らない。

なお 16:125 にも `npm run gate` が出てくるが、こちらは非公開期間中に G3+G5 を
ローカルで回した運用の記述で、用法として正しい。触らない。

### 1-2. 10:307,311 G6 の合否条件

現物の 10:307 は「文脈ゼロの実行体3体」、10:311 は「本物が3体中2体以上で選ばれること」。

実装 `tools/g6-run.sh` は:

- `:25` `N="${G6_READERS:-4}"` — 既定は4体
- `:38` `(( N % 2 == 0 )) || { echo "FAIL: read の体数は偶数にすること…"; exit 1; }` — 奇数は起動しない
- `:157` `NEED=$(( N / 2 + 1 ))`、`:162` 未達なら `FAIL=2`

`:24` のコメントが偶数強制の理由を書いている:
「奇数だと、順番の偏りだけで本物が過半数を取れてしまう（実測: 中身が同じ2本で 2/3 が出た）」。
つまり 10 に書いてある「3体」は、実装が明示的に拒否する設定である。
文書のとおり `G6_READERS=3` で走らせると exit 1 で止まり、
文書の合否条件「3体中2体以上」は永久に評価されない。

10:302 と 10:315 にも「3体」が出るが、こちらは 2026-07-27 の実測結果の記録なので
書き換えない（過去に起きたことの記述であり、これからの合否条件ではない）。

### 1-3. 10:83-85 G0 の進捗行

現物: 「スナップショット生成と G6 実走ランナーは未着手。」（2026-07-26 時点と明記）

`道具検証の記録.md` の見出しは
`## 10. A7 章末スナップショット生成（2026-07-27 実装・実測）`、
`## 11. G6 実走ランナー（2026-07-27 実装・実測）`。
実体も `prototype-chapter/tools/make-snapshot.sh` と `tools/g6-run.sh` として存在する。

ただし「導入済み」と単純に書き換えると別の嘘になる。同 §12 が
G6 の `exec` は共有フックの `npm install` 拒否で止まっており未完走だと書いている。
書き換え文は「4種とも実装・実測済み。ただし G6 の `exec` は未完走」まで含める。

### 1-4. 10:208-211 G4 の観点数

現物の同一段落に次の2文が並ぶ:

- `:208-209` 「観点は固定4つ（技術正確性 / 完成品へのつながり / 退屈度 / 文体逸脱）で
  観点別PASS/FAILを記録し、全観点PASS=合格。」
- `:210` 「★ **「退屈度」の判定方法は G6 の比較評価に一本化する**」

「全観点PASS=合格」で数える観点に退屈度が入ったまま、その退屈度の判定は G4 では
行わないと書いてある。G4 の合否が計算できない。

一方 `:205` は 11 原則7 を根拠に「レビュー観点に『完成品へのつながりが見えるか・
退屈でないか』を必須で含める」と要求している。3つの記述を全部立てるには
**「聞く観点」と「合否を数える観点」を分ける**しかない。
よって合否観点は3つ（技術正確性 / 完成品へのつながり / 文体逸脱）、
退屈だった箇所は G4 でも聞いて記録するが合否には数えない、と書き分ける。

### 1-5. 13:55 の対象文書番号

現物: `- 計画フェーズ中: 文書00〜15それぞれの状態`。
`PROGRESS.md` の「Phase 0: 文書の状態」表は `:95` がヘッダ、`:96` が区切り、
`:97`〜`:113` がデータ行で、00 から 16 までの **17 行**ある（`:113` が 16_決定バックログ）。
13:55 を 00〜16 に直す。

監査の表は「PROGRESS の状態表は 16 行を持つ」と書いているが、実際は 17 行である（§4.1）。

### 1-6. PROGRESS:113 の B群母数

現物 `:113` は「**B群は全30件のうち5件決着・25件が未決定**（内訳: P1 で決める20件／
B13 Phase 2 着手時点／B14 期限切れで再設定／B26 本番1章目の凍結前／
B29・B30 Phase 2 のゲート実装より前。**期限の無い項目はゼロ**）」。

同じファイルの `:25-26` が「**新規に起票した未決定4件**: B31／B32／B33／B34」と書いている。

16 の B 表は `:135`〜`:168` の 34 行（B1〜B34）。うち「いつ」列が `—` のものは
B3（論点ごと消滅）・B12・B16・B17・B20 の5件。残り29件の内訳を数え直すと:

- P1 が 20 件（B1 B2 B4 B5 B6 B7 B8 B9 B10 B11 B15 B18 B19 B21 B22 B23 B24 B25 B27 B28）
- B13・B31 = Phase 2 の道具整備の着手時点
- B14 = 章分割表の凍結前（B1）
- B26・B32・B34 = 1章目の執筆開始前（§1-8 で用語統一後）
- B29・B30・B33 = Phase 2 のゲート実装より前

20 + 9 = 29。「期限の無い項目はゼロ」は 34 件で数え直しても成立する。

### 1-7. 16:283 C群の表ヘッダ

現物のヘッダ: `| # | 事項 | 状態 | 局長の回答（2026-07-26） | 波及 |`。
`f895ab2`（16 の新設コミット）版のヘッダは `| # | 事項 | いつ | なぜ局長か | 推奨案 |` で、
3列目は「いつ」だった。v3 の書き直しで「いつ」が「状態」に置き換わり、期限の置き場が消えた。

その結果、期限は状態セルの中へ紛れ込んでいる:
- C1 は `未回答（P4冒頭）` — 括弧の中
- C2 は `**第2弾の企画に着手する時点まで**（2026-07-28 に期限を設定）` — 状態セルが期限だけを持つ
- C5 の細目の期限は波及セル（`細目（クレジット表記の要否等）は Phase 1 後半`）
- C6 の再検討条件は表の外（`### C6 の再検討トリガー`）

A3「タイミングの無い先送りは禁止」を機械的に見るには、期限が1列に揃っている必要がある。
「いつ」列を6行ぶん復活させる（列の追加なのでヘッダ・区切り・6行の計7箇所を触る）。

### 1-8. 期限用語の統一

grep で拾えた変種は5つあった（監査が挙げた2箇所より多い）:

| 場所 | 現物の表記 |
|---|---|
| 16:160（B26） | `**本番1章目の凍結前**` |
| 16:166（B32） | `**本番1章目の凍結前**` |
| 16:168（B34） | `**本番1章目の執筆前**` |
| 16:360（引き継ぎ#2） | `本番 1 章目の凍結前` |
| 16:361（引き継ぎ#3） | `本番 1 章目執筆前` |
| PROGRESS:43 | `（本番1章目の凍結前）` |
| PROGRESS:113 | `B26 本番1章目の凍結前` |

10 が定義している工程名は G0（執筆環境の完成）/ G1（章分割の凍結）/ G2（文体スペックの凍結）で、
`10:61` の §3 見出しが「G0〜G2: ここを通るまで1文字も書かない」と書いている。
「1章目の凍結」という工程は存在しない。G2 を通った直後が「1章目の執筆開始前」なので、
この語へ統一する。

`material/reviews/codex-round13-14.md:19` にも同じ語があるが、これは 2026-07-28 に
受領したレビューの記録なので書き換えない（過去に何と書いて起票したかの記録）。

**D3 との衝突に注意**: 監査 D3 は B26 の期限を「B1 より前へ」動かすと書いている。
D0 は用語だけを直し、期限そのものは動かさない。D3 が B26 を動かすときは
`章分割表の凍結前（B1）`（B14 が既に使っている表記）へ置き換えることになる。
統合時に D0 と D3 の両方が 16:160 を触るので、順序を決めて当てること。

### 1-9. 12:111-112 の断定形

現物: 「技術的には話者ラベルの一括置換で済み、`13_記録・進捗管理規約.md` §5 の
方針同期チェッカーが置換漏れを機械検出する。」

13:75 の現物は「教材本文がそれと矛盾しないかを検査する `check_policy_sync.py`（仮称）を
**教材執筆開始前に用意する**」。仮称のまま、これから作るものとして書かれている。

実体の確認: リポジトリ全体を `find -name "check_policy_sync*"` で走査してヒット0。
`.github/workflows/gates.yml` のステップにも無い（textlint と embedmd の2本のみ）。

12 は「機械検出する」と現在形の断定で書いており、これを読んだ書き手は
登場人物名の置換漏れが自動で止まると信じる。未実装であることを本文に書く。

### 1-10. 10:31 の main_pm.py 行番号（D0b-3 の一部）

現物 10:31: `| 13 | 3回推敲上限で「警告付きそのまま保存」（main_pm.py L233） | …`

`/home/user/edu-creator/main_pm.py`（437行、コミット `4fcab1b`、作業ツリー変更なし）の
:233 は生成プロンプトの文字列の中で、内容は
`- Start your response with "# Day" (the tutorial heading)`。欠陥コードではない。

実体は2箇所:

```
:52   MAX_REVISION_ROUNDS = 3  # 最大修正ラウンド数
:186            else:
:187                self.logger.warning(f"⚠️ Max revision rounds reached, saving anyway...")
:188                self._save_chapter(chapter_id, draft)
:189                return True
```

前作#13（推敲上限で警告付きそのまま保存）のコードは 2026-08-06 時点で未修正のまま現存する。
`return True` を返すので、呼び出し側は成功と受け取る。

---

## 2. 承認パッケージの差分

`material/承認パッケージ.md` を最後に触ったコミットは `1382805`
（`fix: CodeRabbit 指摘7件を処置し、CIが止まる経路を塞ぐ`）。
そこから現 HEAD 側の `0777f04`（`docs: 前作資産の棚卸しを記録し、未決定4件を起票`）までの差分:

```
material/10_教材制作フロー.md    | 28 ++++++++++++++++++++++
material/12_文体・AI臭さ排除方針.md | 21 ++++++++++++++
2 files changed, 49 insertions(+)
```

削除行は0で、追加のみ49行。中身はゲートを厳しくする方向なので、承認の可否を変える性質ではない。
問題は、局長が承認しようとしている文書と、承認パッケージが説明している文書が
同一でないことのほうである。提出時に上の2行を添える。

（差分の中身の要約と提出そのものは統合役の作業。本書は差分の所在の確定までを行う。）

---

## 3. D0b — 現物の後始末

### 3.1 追跡下の symlink（**実行済み**）

`git ls-files -s` でリポジトリ全体を走査したところ、mode 120000 のエントリは
**1件だけ**だった:

```
120000 42df8df6db2d9ce40fe7356b0c2f6e2b89836e15 0	prototype-chapter/listings/live-reload/node_modules
```

blob の中身は `../expo-first-screen/node_modules`。作業ツリー上も
`lrwxrwxrwx … node_modules -> ../expo-first-screen/node_modules` で一致する。

ルート `.gitignore` は既にこの型を1度踏んでいて、:11-12 に理由が書いてある（対象は :13-14）:

> 末尾のスラッシュを付けると「ディレクトリのみ」の指定になり、同名のシンボリックリンクが
> すり抜ける。実際に検証用リンクをコミットしてしまったので、スラッシュ無しにしてある。

つまり無視パターンは既に直っているが、`.gitignore` は既に追跡済みのファイルを外さないため、
このリンクだけが追跡下に残っていた。
（`prototype-chapter/listings/live-reload/.gitignore` 側は `node_modules/` と
スラッシュ付きで、symlink には当たらない。ルート側の `node_modules` が当たる。）

実行した操作:

```
git rm --cached prototype-chapter/listings/live-reload/node_modules
```

確認:

- `git status --short` → `D  prototype-chapter/listings/live-reload/node_modules`
- `git check-ignore -v …` → `.gitignore:14:node_modules` に当たる（`git add -A` で再追跡されない）
- 作業ツリーの symlink は残っている（捨て試作の `node_modules` 解決経路は壊していない）
- 再走査で mode 120000 のエントリは **0件**

`make-snapshot.sh`:31 の `EXCLUDES` には `node_modules` が入っているので配布物への
現時点の混入は無いが、監査が指摘するとおり除外リストを1行変えた瞬間に前作#1 と同型になる。
追跡から外したことで、除外リストの側の1行に配布物の安全が依存する状態は解消した。

### 3.2 `.github/workflows/` の扱い

現物6本を全部開いて確認した。

| ファイル | 本作での状態 | 判断 |
|---|---|---|
| `gates.yml` | 稼働中（G3 文体・G5 引用） | 残す |
| `trufflehog.yml` | 稼働中（全履歴の秘密走査。D2 の公開判断の前提） | 残す |
| `devin-slack-trigger.yml` | **稼働の根拠あり** | **残す**（監査の分類は不成立。§4.2） |
| `ci-failure-notify.yml` | 監視対象4本のうち3本が存在せず、稼働中の `gates` が入っていない | **監視対象を書き直す**（未実行） |
| `merge-quality-gate.yml` | 常に no-op | **削除**（未実行） |
| `dependabot-automerge.yml` | 起動条件が成立しない | **削除**（未実行） |

根拠:

- `merge-quality-gate.yml` は PR コメント中の `<!-- lighthouse-ci -->` と
  `<!-- protect-main-bot -->` を探す。どちらのボットも本作には無い（Lighthouse を出す
  ワークフローは存在せず、material 全体に `Lighthouse` / `protect-main` のヒットは
  監査本文のみ）。`issues.length > 0` が成立しないので Slack 通知ステップは常に飛ばされる。
  つまり実行はされるが何もしない。
- `dependabot-automerge.yml` は `if: github.actor == 'dependabot[bot]'`。
  `.github/dependabot.yml` が無いので Dependabot は PR を作らず、この条件は成立しない。
  B11（依存バージョンの固定と更新ポリシー）で Dependabot を採るなら、
  設定ファイルとセットで書き直すのが筋で、設定なしの自動マージだけ残す理由が無い。
- `ci-failure-notify.yml`:5-9 の監視対象は `CI` / `Playwright E2E Tests` /
  `TruffleHog Secret Scan` / `Lighthouse CI`。このうち実在するのは
  `TruffleHog Secret Scan` の1本だけで、**`gates` が入っていない**。
  `gates.yml`:20-22 は `push: branches: [main]` を持つので `workflow_run` の
  `branches: [main]` に噛み合うが、名前が列挙されていないため通知されない。
  教材の機械ゲートが main で落ちても誰にも届かない。監査はこの点を挙げていない（§4.2）。

**未実行の理由**: `.github/workflows/` 配下の削除操作がこの実行環境で拒否された。
`ci-failure-notify.yml` の書き換えだけを先に当てると、3本の処置が半分だけ入った状態になる。
3本まとめて1PRで当てるほうが差分が読めるので、本書では判断の記録までとし、
適用は統合役へ回す。当てる内容:

1. `merge-quality-gate.yml` を削除
2. `dependabot-automerge.yml` を削除
3. `ci-failure-notify.yml`:5-9 の `workflows:` を `gates` と
   `TruffleHog Secret Scan` の2本に置き換える

（3 で存在しない名前を残さない理由は、fail open の見え方を1種類に減らすため。
将来 Playwright を足すなら、そのワークフローを追加する同じPRでこの一覧にも足す。）

### 3.3 `GENERICIZATION_PLAN.md` の所在 — **見つからない**

16 B13 の現物（16:147）は「GENERICIZATION_PLAN.md にドラフト済み」と書く。
`PROGRESS.md`:217 は「`docs/edu-creator-generalization-plan` ブランチにコミット済み」と書く。

探した範囲と結果:

| 探し方 | 結果 |
|---|---|
| `find /workspace /home/user -iname "*GENERICIZATION*"` | 0件 |
| sns-app `git log --all --diff-filter=A -- '*GENERICIZATION*'` | 0件 |
| edu-creator 同上 | 0件 |
| task-app 同上 | 0件 |
| edu-creator `git ls-remote --heads origin` | `chore/track-uv-lock-and-singular-folders` / `develop` / `feat/ai-config` / `feature/quality-check-gate` の4本のみ。`docs/edu-creator-generalization-plan` **無し** |
| task-app `git ls-remote --heads origin` | 11本。`docs/edu-creator-generalization-plan` **無し** |
| edu-creator `origin/develop` のルート | `IMPLEMENTATION_ROADMAP.md` など31項目。`GENERICIZATION_PLAN.md` 無し |

リモートまで当たって0件なので、「手元に clone していないだけ」では説明がつかない。
**このファイルは存在しない。**

したがって B13（edu-creator を作るか捨てるか）は、判断材料そのものが無い状態で
期限（Phase 2 の道具整備に着手する時点）を迎える。
本書は B13 に手を出さず、**局長判断キューへ**送る（判断材料が失われている事実を添えて）。

あわせて 10:31 の行番号ズレを §1-10 のとおり直す。前作#13 の欠陥コードが
edu-creator に未修正で現存する事実は、B13 の判断材料の一部になる。

---

## 4. 監査の記述と現物がズレていた箇所

監査を鵜呑みにせず現物を開いた結果、監査の側の誤りが2件出た。
どちらも D0 の修正内容そのものは変えないが、記録に残す。

### 4.1 「PROGRESS の状態表は 16 行」

監査 D0 の表は根拠を `13:55 / PROGRESS.md:95-113` とし、状態表を16行と書いている。
実際は `:95` ヘッダ・`:96` 区切り・`:97`〜`:113` データの **17 行**（文書 00〜16）。
13:55 を「00〜16」へ直すという結論は変わらない。

### 4.2 「`devin-slack-trigger.yml` も他プロジェクト由来で本作に対応物が無い」

監査 D0b の2件目は `merge-quality-gate.yml` と同列に
`devin-slack-trigger.yml` / `dependabot-automerge.yml` を並べている。
`dependabot-automerge.yml` は成立するが、`devin-slack-trigger.yml` は成立しない。

16:353 の見出しが
`## 引き継ぎ（2026-07-30 Devin セッション → 次の Devin または Devincloud）`で、
本作は実際に Devin を使って作業しており、次の担当者としても Devin を想定している。
`devin-slack-trigger.yml` は issue に `devin` ラベルを付けると Slack へ投げる仕掛けで、
その運用に直接対応する。消すと、台帳が想定している引き継ぎ経路のほうが先に無くなる。

**残す。**

あわせて、監査が挙げていない fail open を1件見つけた（§3.2 の `ci-failure-notify.yml`）。
稼働中の `gates` ワークフローが監視対象に入っておらず、main で落ちても通知されない。

---

## 5. 決めなかったこと

- **B13（edu-creator を作るか捨てるか）**: 判断材料 `GENERICIZATION_PLAN.md` が
  存在しない（§3.3）。局長判断キューへ。
- **Slack 連携が実際に動いているか**: `SLACK_BOT_TOKEN` / `SLACK_CHANNEL_ID` /
  `SLACK_DEVIN_USER_ID` が設定済みかはリポジトリの中身からは分からない。
  GitHub Actions の実行記録を見るまで確定できない。
- **B34 の期限**: §1-8 で用語だけ統一したが、12 は G2 で凍結される文書なので
  B34 の実期限は G2 より前になるはずである（§6 の矛盾2）。決め直しは D5 / G2 の作業。
- **workflow 3本の適用**: 判断は §3.2 で確定。削除操作が環境で拒否されたため未実行。

## 6. 見つけた矛盾（決め直さず報告のみ）

1. **16:168（B34）と 10:128・10:55-56**。B34 は「12 に床/天井を入れるか」の期限を
   「本番1章目の執筆前」に置くが、`10:128` の G2 は 12 を凍結する工程で、
   `10:55-56` の大原則4 は「凍結したものを変えるときは工程を巻き戻す」と定める。
   G2 の後に 12 を書き換えるなら大原則4 に従って上流改訂手続きが要る。
   B34 の期限は G2 より前でないと、起票した時点で巻き戻しを前提にしていることになる。
2. **10:135-137 と 12:79-80**。10 の追記は「『prh の辞書1本で足りる』は前作の実態と
   合っていない…B31 の分類で見直す」と書き、12 §3 の1番目は今も
   「check_tone.py移植版: task-appで完成済みの文体チェッカーを sns-app 用に移植」を
   要求している。監査 L159 が既に指摘済みで、処置は D8 の担当。
   §1-9 で 12 §4 を触る際に併せて確認したが、D0 の範囲外なので手を付けていない。
