# 06. API設計書

ステータス: **未着手（Phase 1）**

## 着手条件

`03_基本設計書.md`（画面一覧・ユースケース）と `05_DB設計書.md`（テーブル定義）の
両方が確定した時点で着手する。エンドポイント設計はDBスキーマと画面要求の両方に
依存するため、どちらか一方が未確定な状態で先に書くと手戻りが起きる。

## 着手後にこのファイルへ記載する内容（目次予定）

1. エンドポイント一覧（リソース単位: users / posts / follows / notifications / search 等）
2. 各エンドポイントのリクエスト/レスポンス仕様（OpenAPI形式）。方針決定済み（Codex R1 M18対応）:
   **本文書で仕様を先に固め、実装が従う**。`@nestjs/swagger` の出力は文書との一致照合（検証）に使う
3. 認証方式の詳細（Authorizationヘッダー、リフレッシュトークンのローテーション手順）
4. WebSocketイベント仕様（`04_詳細設計書.md` の内容と重複しない範囲で、
   サーバー側のイベント発行契約として記載）
5. エラーレスポンスの共通フォーマット

## 参照元

- 画面一覧: `03_基本設計書.md` セクション2
- テーブル定義: `05_DB設計書.md`
