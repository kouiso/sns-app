# 16. 決定バックログ（実装前に決め切る事項の一元台帳）

ステータス: ドラフト（局長レビュー待ち）
役割: 実装（Phase 3 伴走）に入る前に決着が要る**全事項**を、誰が・いつ決めるかで
一覧化する。各文書に散らばっていた「Phase 1で確定」「局長判断」を1枚に集約し、
抜け漏れと「会話の中だけの決定」を防ぐ（`13_記録・進捗管理規約.md` §1: ファイルに
落ちて初めて決定 / §2 ADRと対になる**未決定側の正本**）。

## 凡例

- **決める人**: 自分たち（副長＋レビュー担当） / 局長
- **いつ**: P0=今この計画固めで / P1=詳細設計 / P2=道具整備 / P3=伴走 / P4=販売 / 後続=シリーズ2弾以降
- **状態**: 決定（本台帳で確定） / 先送り（＝工程上そこで決めるのが正しい。idleではない） / 局長確認

「先送り」と「idle（怠慢）」は違う。先送り項目には必ず決定タイミング（いつ）を付ける。
タイミングの無い先送りは禁止（＝実質の未決扱い）。C群にも「いつ」を必ず付す。

---

## A. 本台帳で確定した事項（自分たちで決定）

### A1. 未決定事項の管理方法
本書（16）を**未決定事項の唯一の正本**とする。散在記述は本書へ集約し、各文書は本書を参照する。

### A2. 登場人物名（磯貝／阿部）
Phase 3 開始まで**暫定名として確定**。一括置換できる設計は維持（12 §4）。変えたくなったら差し替える。

### A3. 「先送り」の運用
先送り項目には決定タイミングを必ず付す。無タイミングの先送りは未決扱いに落とす。

### A4. Phase 3 の実装主体と「初心者制約モード」 ★最重要
**問題**: 開発ログ（10 §5）は教材の対話・エラー体験の唯一の原料。AIが手癖で実装すると
記録される詰まりが「AI固有のハマり」になり、初心者が実際に踏む詰まりと別物になる。
大原則1/2（本来の開発フローをなぞる）の土台が空洞化する。

**決定**:
1. 実装の手を動かすのはAI（Codex/Claude）でよい。ただし **「初心者制約モード」** を課す。
2. 初心者制約モードの規則:
   - その章の教材で**まだ教えていない知識・道具を先取りして使わない**（未導入のライブラリ、
     未説明のCLIオプション、未学習の型テクニックを「知っているから」で使わない）
   - エラーが出たら**まずエラーメッセージだけを頼りに1手打つ**。その1手と結果を必ず記録する
   - 公式ドキュメント・エラーメッセージ・既に教材で教えた知識のみを一次情報とする
   - 内部知識で一発正解を書いた場合は、**そこは「詰まりなし」として記録しない**のではなく
     「AIが先回りしたため初心者の詰まりは未観測」と明記する（後述A5の検証で拾う）
3. 開発ログには **friction種別ラベル** を必ず付ける:
   - `beginner`（初心者が確実に踏む。教材の対話に採用する）
   - `ai-artifact`（AI固有・環境固有。教材には採用しない）
   - `unobserved`（AIが先回りして観測できなかった。A5のG6で埋める）
4. `ai-artifact` だけで構成された章は、対話素材が不足しているとみなしG3で警告する。

### A5. G6実走の実行主体＝知識隔離の担保 ★最重要
**問題**: G6（教材実走検証）の存在意義は「教材に書いていない暗黙の前提」の検出。
その章を書いた本人（または同一文脈のAI）が走らせると暗黙知で補完して必ずPASSし、
最も恐れている「実際にやったら詰まる教材」をゲートが素通しする。

**決定**:
1. G6実走は **執筆に関与していない、文脈ゼロの実行体**が行う。同一セッション・同一
   会話文脈での実走は**G6として無効**とする。
2. 実行体に渡してよい入力は **その章までの教材テキストと、章開始時点のスターターのみ**。
   設計書・開発ログ・実装リポジトリを見せてはいけない。
3. 実走中に実行体が**教材外の知識で補完して進んだ箇所**は、それ自体を欠落として記録する
   （「進めたからPASS」ではない）。
4. 実走の結果は10 §4のG6RUN受領証として台帳に残す（既存の受領証契約を流用）。
5. A4の `unobserved` ラベルが付いた箇所は、このG6で詰まるかどうかを**重点確認項目**として渡す。

### A6. G4のAIファミリー割当
**決定**: 生成側＝Claude、レビュー側＝別ファミリー（Codex/GPT系）、代替＝さらに別系統。
「別ファミリーであること」が要件であり、具体的なモデル名は入手可能性で変わるため
G4実行時に**実際に使ったファミリー名を記録する**ことをもって充足とする。
自分の生成物を同一ファミリーでレビューして合格にする経路は禁止。

### A7. 章別チェックポイント（詰まったときの復帰点）
**問題**: ペルソナB（独学者）は質問相手ゼロ。復帰手段が無いと1章の詰まりで完走不能になる。
一方これは前作#1「販売ZIPに完成品混入」の火元そのもの。

**決定**:
1. 章末スナップショットを**配る**（完走率を優先）。
2. ただし**手作業で作らない**。各章タグから機械生成する。
3. **教材本体とは別配布**にし、開始状態スターターへの混入検査（G0成果物）は従来どおり厳格に維持する。
   「配布物が2種類ある」ことを配布形式（B9）の設計前提に含める。
4. スナップショットには「ここまでの完成コード」であることと、**先の章のコードを含まない**ことを機械検査する。

### A8. コード規約の凍結方針（G5の偽FAIL/偽PASS防止）
**決定**:
1. フォーマッタ＝Prettier（設定は原則デフォルト）、リンタ＝ESLint。
2. コード内コメントは**日本語**（教材の読者が日本語話者のため）。
3. **G5の機械diffは、フォーマッタ適用後の正規化テキストで比較する**。整形差だけでFAILさせない。
4. 具体の設定値（行幅・セミコロン等）はP1で確定し、確定後は伴走中に変更しない。
   変更が要る場合はADR＋全章のG5再実行をセットで行う。

### A9. テストコードをカリキュラムに含めるか
**決定**: **含める**（ただし最小限）。
- 理由: 実装PRのマージ条件がテスト緑（10 §4）である以上、著者リポジトリには全章テストが
  存在する。教材が一切教えないと学習者の手元と章タグのリポジトリが構造的に乖離し、
  大原則1（本来の開発フローをなぞる）と矛盾する。
- 範囲: 「テストを網羅的に書く」ではなく「**壊れたことに気づける最小のテスト**」を教える。
  E2E（Playwright）は主要動線1本に絞る。
- 章位置と分量はP1の章分割（B1）で確定する。

### A10. GitHub／CI基盤の決定タイミング
**決定**: **Phase 2開始までに用意する**（ゲート常設の前提のため。C3から「いつ」を確定）。
- ホスティング＝GitHub。**リポジトリは公開**（C3 で局長が2026-07-26に決定）。
- CI実行基盤は **GitHub Actions に確定**（public のため無料枠が使える）。
  Blacksmith は不要になった。
- ブランチ保護とPRマージ条件はゲート設計（10 §4）の前提なのでPhase 2で有効化する。
- **★ 2026-07-28 実測: 非公開リポジトリでは GitHub Actions が動かなかった。**
  「支払いに問題があるためジョブを開始できない」と返る。非公開の Actions は無料枠に
  上限があるため。**枠を増やす選択は取らない。** 当面はローカルで `npm run gate` を回し、
  結果を PR 本文へ書く運用にする。
  **この失敗そのものは「public なら無料枠で走る」の証拠にならない**。
  「支払いに問題がある」は、無料枠を使い切ったことも、公開にすれば走ることも示さない。
- **★ 2026-07-28 実測（こちらが公開実行の証拠）: 公開に切り替えたあと、
  同じワークフローが3回とも成功した**（`gates` ジョブ / commit ead9efc・a51cbe5・fa6e26c）。
  課金の設定は何も変えていない。よって「public なら無料枠で走る」は
  **実際に走らせて確かめた**。**公開そのものは C3 で 2026-07-26 に決定済みであり、
  ここで改めて可否を問う論点は残っていない**（2026-07-28 に公開へ切り替え済み）。
  C6（教材本文は別の非公開経路で配る）は公開と両立する話であって、公開を止める条件ではない。
- **実装リポジトリが公開になる以上、教材本文は必ず別の非公開経路で配る**（C6 と連動）。

---

## B. 工程上あとで決める（先送り＝順序。idleではない）

| # | 事項 | いつ | 現状・初期案 | 由来 |
|---|---|---|---|---|
| B1 | **章分割表の確定**（骨子15→凍結） | P1 | 15のパート構成（A0〜E）を親に、04の依存関係と「本来の開発フローの順序」から導出 → 10のG1で凍結。A9（テスト章）もここに載せる。**章数の目安は捨て試作の実測後に15へ記入する**（旧47〜58章は構成変更で無効） | 14, 10 G1 |
| B2 | **実装リポジトリ／プロジェクト構造** ★抜け | P1 | **書き直し（2026-07-26）**。旧初期案の monorepo（`apps/web`=React+Vite, `apps/api`=NestJS）は前提ごと消滅。新初期案: **1リポジトリ・単一 Expo アプリ**（`app/`=Expo Router の画面, `supabase/`=マイグレーションと RLS ポリシー, `material/`同居）。**常駐する自前のアプリケーションサーバーが無くなったため**構造が大幅に単純になった。ただし `supabase/` 配下のマイグレーション・RLS ポリシー・トリガー・Storage ポリシーは**実行されるサーバー側のコードであり、本構成の認可の境界そのもの**である。レビュー・テスト・配布の対象から SQL を落とさない（2026-07-27 の別ファミリーレビュー指摘。旧文言「バックエンドのコードが存在しない」を訂正） | D1 |
| B3 | ~~開発環境（Docker Compose）の具体構成・対応OS・ポート~~ **← 消滅（2026-07-26）** | — | Redis / MinIO / NestJS が消え、ローカルで起動するミドルウェアが無くなったため論点ごと消滅。開発環境は「Node と npm と Expo Go」だけになった。対応OS の論点は B15 へ引き継ぐ | 04 項目5, 02 |
| B4 | タイムライン生成 プッシュ型／プル型 | P1 | 初期案プル型（**Redis 補完は消滅**。05 §3 から該当記述を削除済み） | 05 §3 |
| B5 | **実機ビルド・配布をどこまでやらせるか**（Expo Go 止まりか、EAS Build まで） | P1 | **書き直し（2026-07-26）**。Capacitor のビルドパイプラインは消滅。新論点は「Expo Go で動かすところまでで教材を終えるか、EAS Build で自分の署名済みアプリを作るところまでやらせるか」。前者なら挫折ポイントゼロを維持でき、後者なら実務に近い | 02 §7, 03 §5 |
| B6 | **Web 版の公開先**（パートD の Expo web 出力をどこに置くか） | P1 | **書き直し（2026-07-26）**。旧「早期公開体験」は構成変更で位置が変わった（早期＝スマホ実機に置き換わり、Web 公開はパートD へ移動）。HTTPS・秘密情報の扱い・後片付けの設計は残る論点 | 15 パートD |
| B7 | UC1「新規投稿のフォロワー通知」を通知種別に含めるか | P1 | notifications.type に含めるか判断 | 05 §4課題1 |
| B8 | 05 §4 の残り未解決課題（計6点） | P1 | Phase 1完了条件 | 05 §4 |
| B9 | 配布形式（ZIP／Webサイト等） | P1 | **A7により「本体＋章末スナップショット」の2系統配布が前提条件**。11 §5参照 | 14, A7 |
| B10 | **デモ／シードデータ方針** ★新規 | P1 | 単一アカウントではタイムライン・検索・通知の章で「見える変化」が作れない。初期案: seedスクリプトを教材内で教える（章順に食い込むためB1と同時決定）。素材画像は権利処理済みのものに限る（C5と連動） | 敵対レビュー |
| B11 | **依存バージョンの固定値と更新ポリシー** ★新規 | P1 | **書き直し（2026-07-26）**。Xcode / Android Studio / JDK のツールチェーン版という最大のリスク源が消えた（Expo Go は学習者側に開発ツールを要求しない）。残る対象は **Expo SDK のメジャー版**と Node の版、および **Supabase 側の仕様変更**（こちらは lockfile で止められず、リポジトリのHEADも変わらないためG5では検知できない → 定期的なG6再実走で検知する）。P1終了時点の安定版でピンし02に版の表を作る | 敵対レビュー, 14リスク表 |
| B12 | ~~**認証機能のスコープ確定**~~ **← 決着（2026-07-26）** | — | C4 で局長が「メール確認・パスワード再発行の両方を入れる」と決定。01 に FR13・FR14 として追記済み（01 は再承認が必要）。**残る派生論点は B14**（Supabase のメール送信が1時間2通の制限に当たるか） | 敵対レビュー → C4 |
| B13 | edu-creator 改修の実装 | **Phase 2 の道具整備に着手する時点まで**（2026-07-28 に期限を設定） | GENERICIZATION_PLAN.md にドラフト済み。局長方針で**実装は保留**（2026-07-25「実装はまだやめたほうがいい」）。設計は据え置き。**「保留継続」だけでは A3 の「タイミングの無い先送りは禁止」を満たさないので、期限を Phase 2 の着手時点に置いた。**その時点で既製品の組み合わせだけで足りるかが分かるため、作るか捨てるかを判断できる。**役割は「全部自作の生成器」から「既製品を束ねる薄い司令塔」へ変更予定**（14 Phase 2 と連動） | 10 §6 |
| B14 | **カスタム SMTP の要否** ★新規（2026-07-26） | **⚠️ 期限切れ → 章分割表の凍結前（B1）へ再設定（2026-07-28）** | Supabase 標準のメール送信は**1時間に2通まで**。複数アカウントを作る演習（タイムライン・通知・フォローの章）が詰まる可能性がある。実際に何通目で止まるかを確かめてから決める。**推測で決めない**（A3「タイミングの無い先送りは禁止」）。**元の期限は「捨て試作で実測」だったが、捨て試作は 2026-07-27 に完了したのに測っていない。期限を守れなかった事実をここに残す。**再設定した期限が章分割表の凍結前である理由は、演習に要るアカウント数が決まる時点で上限に当たるか判明していないと章を設計できないため | 02 §5 R2 |
| B15 | **対応OS の範囲** ★新規（2026-07-26） | P1 | 旧 B3 から引き継いだ論点。Expo は Mac 無しでも Android 実機で完走できるが、iOS 実機で確認するには Mac が要るのか（Expo Go 経由なら不要）を確定し、08 の記述と揃える | 旧B3 |
| B16 | ~~**Expo SDK の版を教材で固定するか**~~ **← 決着（2026-07-27）。§B-P0 決定を見よ** | — | 捨て試作の G4 で判明。`npx create-expo-app@latest` は SDK 版を固定しない。**固定すると教材が古くなり、固定しないと読者の手元と教材のコードが一致しなくなる**。選択肢は (a) 版を固定して改訂時に上げる (b) 固定せず「版が違うと画面が変わることがある」と書く (c) `listings/` 側だけ固定して本文は固定しない。**(a) と (c) は C-6（改訂版をどう届けるか）と直結する** | 10 §4 G5, C-6, `prototype-chapter/道具検証の記録.md` §9 |
| B17 | ~~**物理端末の Expo Go が対象 SDK を受け入れるか**~~ **← 決着（2026-07-27）。§B-P0 決定を見よ。ただし実機での最終確認だけ残る** | — | 別ファミリーレビュー指摘。App Store 版の Expo Go は最新 SDK に追いつくまで時間差があり、**SDK 57 を選ぶと物理端末では動かない可能性がある**（シミュレータでは動作を確認済み）。教材の最初の成功体験（QR を読んで自分の端末に画面が出る）が直撃する。選択肢は (a) Expo Go が対応済みの SDK に合わせる (b) development build を作る章を足す。**B16（版の固定）とセットで決める** | 02, 15 パートA0, B16, B5 |
| B18 | **メール確認のリダイレクト先をどう作るか** ★新規（2026-07-27） | P1 | 別ファミリーレビュー指摘。FR13（メール確認）はメールのリンクからアプリへ戻す必要があり、Expo Go だけで完結するとは限らない。B5（実機ビルド）の論点を「EAS Build を教えるか」から「認証の確認用にどの時点で専用ビルドを作るか」へ書き換える | 01 FR13, 03 UC3, B5 |
| B19 | **DB 側の既定値（UUID・作成日時）を誰が入れるか** ★新規（2026-07-27） | P1 | 別ファミリーレビュー指摘。旧構成では ORM とサーバーが埋めていた。クライアントから直接 insert する構成では、DB に `gen_random_uuid()` / `now()` を持たせるか、全 insert でクライアントが入れる契約にするかを決める必要がある | 05, 06 |
| B20 | ~~**`auth.users` トリガーが `users` 行を作れるか**~~ **← 決着（2026-07-27）。§B-P0 決定を見よ** | — | 別ファミリーレビュー指摘。`users` の `username` / `display_name` が NOT NULL かつ既定値なしなのに、登録要件はメールとパスワードだけ。**トリガーが失敗すると新規登録そのものが失敗する**。登録画面で username を取るか、トリガー側で仮の値を作るかを決める | 01 FR1, 05, 03 UC1 |
| B21 | **画像のサイズ・形式をサーバー側で止めるか** ★新規（2026-07-27） | P1 | 別ファミリーレビュー指摘。現在は画面入力の検証だけで、改造したクライアントは迂回できる。Storage の bucket 側で MIME と容量の上限を設定するかを決める | 01 非機能, 05 Storage |
| B22 | **Realtime の取りこぼしをどう埋めるか** ★新規（2026-07-27） | P1 | 別ファミリーレビュー指摘。Realtime は全変更の配信を保証しない。購読開始前の初回取得・再接続後の再取得・重複排除を仕様に入れるかを決める | 01 FR9, 03, 04 |
| B23 | **タイムライン取得の性能目安を何で測るか** ★新規（2026-07-27） | P1 | 別ファミリーレビュー指摘。旧「ローカル開発環境で200ms以内」は自前APIが手元で動いていた頃の残骸で、ホスト型 Supabase になった今は再現できない。**DB 側の実行時間**と**利用者の体感時間**を分けて測り、リージョンと回線条件を固定した端末実測値へ置き換える。数値そのものは捨て試作か Phase 1 の実測で決める（推測で置かない） | 01 §3, 08 項目9 |
| B24 | **表示ID（username）を最終的に必須にするか** ★新規（2026-07-28） | P1 | Round 13 指摘。B20 の決定で `username` を NULL 可へ変えた結果、**未設定のまま残る利用者が存在しうる**。後から NOT NULL を付けるのか、画面の誘導だけで済ませるのかを決める。`05_DB設計書.md` の users 節に書いてあったが本台帳に無く、**この台帳が未決定の唯一の正本だという前提を破っていた** | 05 users / B20 |
| B25 | **表示IDが未設定の利用者を他人からどう見せるか** ★新規（2026-07-28） | P1 | Round 13 指摘。B24 と同じ穴。タイムライン・プロフィール・検索結果で何を出すかが決まっていない。表示IDが空だとプロフィールのURLも作れない | 05 users / B20 / 03 #13-2 |
| B26 | **ストアの Expo Go が SDK 54 の企画を実際に開けるか（物理端末での確認）** ★新規（2026-07-28） | **本番1章目の凍結前** | Round 13 指摘。B17 は「決着」としたが、**物理端末で画面が出たことは一度も見ていない**（シミュレータのみ）。教材1章目の成功体験そのものなので、決着扱いのまま期限無しで置くのは危険。B17 から切り出して期限付きで開き直す | B17 / 03 |
| B27 | **削除済み投稿を DB 自身に隠させるか、画面側の絞り込みに任せるか** ★新規（2026-07-28） | P1 | Round 16 指摘。`05_DB設計書.md` §5 に「Phase 1 で確定」と書いたまま本台帳へ載せていなかった。**この台帳が未決定の唯一の正本だという前提を破っていた**（A1）。論理削除した投稿を SELECT ポリシーで弾くか、アプリ側の絞り込みに任せるかを決める。後者は改造したクライアントに素通しされる | 05 §5 |
| B28 | **通知を作る DB トリガーの実装範囲** ★新規（2026-07-28） | P1 | Round 16 指摘。B27 と同じ漏れ。どの操作（いいね・リプライ・リポスト・フォロー）まで DB のトリガーで通知行を作るかを決める。03 UC2 の方式の具体化にあたる | 05 §5, 03 UC2 |
| B29 | **G5 の「引用範囲の過不足」を検査する道具を作るか** ★新規（2026-07-28） | **Phase 2 のゲート実装より前** | Round 16 指摘。G5 は引用範囲の**内側**しか見ないので、`listings/` の実ファイルが範囲の外で変わっても緑のままになる（`prototype-chapter/道具検証の記録.md` §14 で実測）。**読者は教材に載っていない行を持つファイルを受け取る。**この偽の緑を抱えたまま Phase 2 でゲートを必須化すると、必須化した意味が薄れる。作るなら「listings のファイル全体が、章のどこかの引用範囲で覆われているか」を数える検査になる | 道具検証の記録 §9・§14 |

---

## B-P0 決定: B16 / B17 / B20（2026-07-27）

P0 の3件を、公式資料と実測で確定させた。**推測で決めた項目はひとつも無い。**

### B17・B16 — 教材は **Expo SDK 54** を対象にし、作成コマンドで版を固定する

**決定した作成コマンド**:

```text
npx create-expo-app@latest <プロジェクト名> --template default@sdk-54
```

**なぜ 54 か（実測と一次資料）**:

| 確かめたこと | 結果 | 確かめ方 |
|---|---|---|
| App Store の Expo Go の版 | **54.0.2**（2025-09-23 公開、React Native 0.81.1） | Apple の公開情報を照会 |
| Google Play の Expo Go の版 | **54.0.8**（2026-05-12 更新） | ストアの掲載情報 |
| Expo SDK の最新版 | **57.0.8** | npm の登録情報 |
| Expo 公式の案内 | 「**物理端末で Expo Go を使うなら SDK 54 のプロジェクトを使うこと**」と明記 | `docs.expo.dev` の作成手順ページ（2026-07-22 更新） |
| Expo 自身の説明 | SDK 55・56・57 版の Expo Go は「**Apple の審査待ちで、時期は示せない**」 | Expo の更新履歴（2026-05-04 / 05-21 / 06-30） |

つまり **ストアから入る Expo Go は今も SDK 54 のまま**であり、SDK 57 で作ったプロジェクトは
学習者の端末で開けない。教材の1章目は「QR を読んで自分の端末に画面が出る」ことが
成功体験そのものなので、ここを外すと章が成立しない。

**★ 公式ドキュメントの記述と実測が食い違った（2026-07-27 実測）**

Expo のドキュメントには「移行期間中、`--template` を付けなければ SDK 54 のプロジェクトが
できる」と書いてある。**実際に走らせたら SDK 57 ができた。**

| 実行したコマンド | できたもの |
|---|---|
| `create-expo-app@latest --template default@sdk-54` | **expo 54.0.35 / React Native 0.81.5** |
| `create-expo-app@latest`（固定なし） | **expo 57.0.8 / React Native 0.86.0** |

（`create-expo-app` 4.0.0、対話なしの状態で実行。対話ありだと選択肢が出る可能性は残る）

**この食い違いが選択肢を1つ潰した**。「固定せず、版が違えば画面が変わると書いておく」案は、
学習者が素直にコマンドを打った時点で**開けないプロジェクトができる**ため採れない。
`listings/` だけ固定する案も、失敗するのはコードを見る前の作成時点なので効かない。
**残るのは版を固定する案だけ**であり、消去法ではなく実測で1つに絞られた。

**有効期限を決めておく**（決めないと静かに腐る）:

- Expo は SDK をおよそ年3回出し、各 SDK の寿命はおよそ1年。**SDK 54 への修正提供は
  次の SDK が出る 2026年9〜10月まで**と Expo 自身が書いている。
- **2026年9月に再確認する**。新しい Expo Go がストアに並ぶと、学習者の Expo Go は
  自動で更新され、**SDK 54 のプロジェクトは開けなくなる**（Expo Go は最新 SDK しか
  対応せず、iPhone では古い版へ戻す手段が無い）。
- そのときの逃げ道は「専用ビルドを作る章を足す」。B5・B18 と同じ論点に合流する。

**教材に必ず書くこと**: QR が読めなかったときの対処。(1) プロジェクトの SDK 版を確かめる、
(2) Android なら Expo の案内から対応する Expo Go を入れ直せる、(3) 手元に端末が無ければ
シミュレータで進める。**有料の Apple 開発者登録が要る方法は初心者の道として出さない。**

**Node の版**: SDK 54 が要求するのは **Node 20.19 以上**（SDK 57 の 22.13 以上ではない）。
02 の版の表はこちらに合わせる（B11 と連動）。

**★ この決定は捨て試作の作り直しを要求する（2026-07-28 実測で判明）**

決定した直後に、既にある捨て試作の中身を確かめたところ、**全部 SDK 57 で作られていた**。

| 対象 | 実際の版 |
|---|---|
| `prototype-chapter/listings/expo-first-screen` | expo ~57.0.8 |
| `prototype-chapter/listings/live-reload` | expo ~57.0.8 |
| `prototype-chapter/snapshots/expo-first-screen` | expo ~57.0.8 |
| iOS シミュレータの Expo Go が表示した版 | **SDK version: 57.0.0**（画面で確認） |

**つまり捨て試作の2章は、ストア版 Expo Go では開けないプロジェクトを教材化していた。**
シミュレータでは開けるので、シミュレータだけで検証していると最後まで気づかない。
**この1件が「シミュレータの緑は実機の証拠にならない」の実例である。**

対応: 章分割が凍結する前に、`listings/` と `snapshots/` を SDK 54 で作り直す。
捨て試作は商品にしないため今すぐ直す必要はないが、**本番の1章目を書き始める前には必ず直す**
（G0 の道具が揃った時点のチェック項目に入れる）。

**残る確認（実機が要る）**: 実際の iPhone と Android で、ストア版 Expo Go が SDK 54 の
プロジェクトを開けることの確認。**この確認はまだ取れていない**（局長の指示で実機を使わず、
シミュレータのみで検証したため）。上の表は「ストアに並んでいる版が 54 である」ことまでを
確かめた段階であり、**実機で画面が出たことは観測していない**。

### B20 — トリガーは行だけ作り、プロフィールは初回ログイン後に本人が入れる

**決定**: `users` の `username` / `display_name` から NOT NULL を外し、`updated_at` に既定値を持たせる。
トリガーは id と日時だけを埋める。username と display_name はプロフィール設定画面で本人が入れる。

**なぜそうしないと壊れるか**: 登録画面で受け取るのはメールとパスワードだけ（01 FR1）なので、
トリガーが動く時点で username に入れる値が無い。NOT NULL のままだとトリガーの挿入が落ち、
**落ちると登録処理そのものが取り消される**。Supabase 自身が「トリガーが失敗すると
登録を止めうるので十分に検証すること」と警告しており、PostgreSQL の仕様としても
AFTER トリガーはエラーで操作全体を中断する。

**Supabase 公式の手順と同じ形である**ことも確認した。公式が公開しているプロフィール表は
username を含めて**主キー以外が全て NULL 可**であり、Expo 向けの公式チュートリアルでも
プロフィールは登録後の画面から入れている。

**利用者メタデータを信用しない**: Supabase は「登録時に渡す利用者メタデータは
**本人が後から書き換えられる**ので、権限の判断に使う場所ではない」と明記している。
username の一意性は DB の UNIQUE 制約で受け止め、違反をそのままアプリへ返す。
トリガーの中で重複させると「登録に失敗しました」としか出ず、原因が読めない。

詳細と波及先は `05_DB設計書.md` の users テーブルの節に書いた。

---

## C. 局長判断が要る事項（最後にまとめて確認）

**C3〜C6 は 2026-07-26 に局長が回答済み。以下は回答後の状態。**

| # | 事項 | 状態 | 局長の回答（2026-07-26） | 波及 |
|---|---|---|---|---|
| C1 | 販売チャネル・価格 | 未回答（P4冒頭） | 今は判断しない | — |
| C2 | 各教材の想定期間／シリーズの作り方 | **第2弾の企画に着手する時点まで**（2026-07-28 に期限を設定） | 局長は「今は判断しない」と回答済み。ただし**「後続」では A3 の「タイミングの無い先送りは禁止」を満たさない**ので、期限を milestone で置き直した。第2弾の企画を書き始める時点で、本作の実績期間が出ているためそれを材料に判断できる | 09の論点として保持 |
| C3 | GitHubリポジトリの公開／非公開 | **回答済み** | **公開する**（受注側の推奨＝非公開とは異なる） | CI基盤が **GitHub Actions に確定**（public なら無料枠が使えるため、`no-paid-infra-without-value` と整合）。**完成コードが公開されるため、教材本文は別の非公開経路が必須** → C6 と連動。学習者が Issue で質問できる副次効果 |
| C4 | 認証スコープ（メール確認・パスワード再発行） | **回答済み** | **両方入れる**（受注側の推奨＝確認は外す、とは異なる） | 01 に FR13・FR14 追記 → **01 は再承認が必要**／03 の画面 12→15／02 §5 にディープリンクとメール送信制限を明記／08 にメール到達・トークン期限のテスト観点／B12 決着・B14 新設 |
| C5 | 学習者の成果物の公開可否とライセンス | **回答済み（仮確定）** | **許可**（推奨どおり）。細目は優先度低として後送り | 細目（クレジット表記の要否等）は Phase 1 後半。**優先度低と明示** |
| C6 | 販売後の改訂配布経路 | **回答済み（暫定運用）** | **今は Drive のフォルダ共有で進め、売れ始めてから考え直す** | ZIP 配布は採らない（フォルダ共有なら差し替えが全員に届く）／サイト構築は不要 |

### C6 の再検討トリガー（受注側が決定・承認不要）

A3「タイミングの無い先送りは禁止」を満たすため、再検討の条件を数値で固定する。

> **(a) 購入者20人超、または (b) 改訂版を配る必要が実際に発生したとき、の早い方。**

(b) を含める理由: C6 が実際に問題化するのは人数ではなく
「直した版を届けなければならない事態が初めて起きたとき」だから。

---

## D. 本書で発見した「文書にすら無かった抜け」

| # | 抜け | 証拠 | 反映アクション |
|---|---|---|---|
| D1 | 実装リポジトリ／プロジェクト構造の記述が**どこにも無い** | 再検証済み: `grep -riE "構成\|ディレクトリ\|リポジトリ\|配置" material/` の全ヒットを目視 → 実装リポジトリは「存在する前提」で参照されるのみ、その**構造を決める記述はゼロ**。04の目次にも項目なし | B2としてP1で確定。04の目次に「プロジェクト構造」項目を新設 |
| ~~D2~~ | ~~開発環境（Docker Compose）の具体構成が未記述~~ **← 取り下げ（誤検出）** | 初回grepが `docker-compose`（ハイフン）で検索したため、実記述の「Docker Compose構成」（半角スペース）に当たらなかった。再検証で04目次項目5に存在を確認 | 取り下げ。B3として正しい先送りに分類し直した |

**この誤検出から得た教訓**: 日本語文書のgrepは表記ゆれ（ハイフン/スペース/全角半角/送り仮名）で
簡単に取りこぼす。**「ヒット0」を根拠に「記述なし」と断定しない**。断定の前に語を分解した別
パターンで再検索し、関連文書の目次を目視する。→ `13_記録・進捗管理規約.md` §2.1 に規約化済み。

---

## E. 本台帳に対する敵対レビューの記録

- 2026-07-25 第1回（別コンテキストのレビュー担当・Opus）: 12件の未計上決定を検出。
  うち **A4〜A10 の7件を本台帳で確定**、**B10〜B12 の3件をP1先送り**、
  **C4〜C6 の3件を局長確認キュー**へ振り分けた（1件はA10とC3に分割）。
  誤検知チェックも実施され、9件が「既にアンカー済み」として棄却されている
  （Docker Compose構成→04項目5、Windows対応→08項目7、早期公開ホスティング→04項目6 ほか）。
- 検証手段の制約: Codex CLI を2回試行したが、いずれもセッション初期化中に終了して判定が
  得られなかった（skill読み込みエラーとMCP認証切れをログで確認）。そのため別コンテキストの
  レビュー担当で代替した。**別ファミリーによる再検証はA6の方針に沿ってPhase 2で実施する**。

## 更新履歴

- 2026-07-25 作成。grep証跡ベースで各文書の未決定を集約＋抜けD1を発見（D2は誤検出のため取り下げ）。
- 2026-07-25 v2。敵対レビュー12件を反映。A4（実装主体＝初心者制約モード）・A5（G6の知識隔離）・
  A7（章別チェックポイント）を新規確定。C群に「いつ」列を追加（A3の自己違反を是正）。
- 2026-07-26 v3。技術構成が Expo + Supabase へ変わったため B群を書き直し。
  **消滅2件**（B3 Docker Compose 構成／B12 認証スコープ＝C4で決着）、
  **書き直し4件**（B1 章数・B2 リポジトリ構造・B5 実機ビルド・B6 Web公開先・B11 版固定）、
  **新設2件**（B14 カスタムSMTPの要否＝捨て試作で実測／B15 対応OSの範囲）。
  C群は C3〜C6 に局長回答を記録し、C6 の再検討トリガーを数値で固定した。
  A10 は C3 の回答を受けて「公開・GitHub Actions」に確定。
- 2026-07-27 v4。**新設1件**（B16 Expo SDK の版を教材で固定するか）。
  捨て試作の G4 別ファミリーレビューで、`create-expo-app@latest` が版を固定しないことが
  指摘されて判明した。C-6（改訂版の届け方）と直結するため P0 とした。
- 2026-07-27 v5。別ファミリー（Codex）によるスタック変更差分レビュー13件を受領
  （`reviews/codex-round12-stack-pivot.md`）。うち **05 の RLS 2件は本文へ反映済み**
  （列を絞れない／DELETE を論理削除に変えられない）、**キーの取り違え1件も 05 へ追記**。
  残りは B17 として起票し、B16 に前提条件を追記した。
- 2026-07-27 v6。**v5 の処置に取りこぼしが4件あった**ため補完した（Round 12 の13件全件の
  行き先を下表に固定する）。**新設1件**（B23 タイムライン性能目安の測り方）。
  文書側の直接修正3件: 04 の目次に「Supabase クライアントの初期化とセッション寿命管理」を
  追加（指摘7）／16 B2 の「バックエンドのコードが存在しない」を「常駐する自前サーバーが無い」
  へ訂正し、`supabase/` 配下の SQL が認可の境界であることを明記（指摘13）／01 §3 と 08 項目9 の
  200ms 目安を「再定義中（B23）」へ差し替え（指摘12）。指摘10（第3弾の再利用範囲）は
  09 で処置済みであることを確認した。

### Round 12（13件）の行き先

| 指摘 | 重大度 | 行き先 |
|---|---|---|
| 1 メール確認と Expo Go の両立 | C | B18 |
| 2 物理端末の Expo Go と SDK 版 | C | B17（B16 に前提を追記） |
| 3 クライアントへ置くキーの種類 | C | 05 へ反映済み |
| 4 RLS は列を絞れない | C | 05 へ反映済み |
| 5 論理削除と DELETE 権限の矛盾 | C | 05 へ反映済み |
| 6 `auth.users` トリガーが users 行を作れない | C | B20 |
| 7 RN クライアントの寿命管理が目次に無い | M | **04 の目次へ追加（v6）** |
| 8 UUID・作成日時の既定値の担当 | M | B19 |
| 9 画像の MIME・容量をサーバー側で止める | M | B21 |
| 10 第3弾「フロントは1行も変えない」の矛盾 | C | 09 へ反映済み |
| 11 Realtime の配信保証なし | M | B22 |
| 12 200ms 目安の陳腐化 | M | **B23 新設＋01・08 を修正（v6）** |
| 13 「バックエンドのコードが存在しない」は誤り | m | **16 B2 の文言を訂正（v6）** |

- 2026-07-27 v7。**P0 の3件（B16 / B17 / B20）を確定**（§B-P0 決定）。一次資料の確認に加えて
  作成コマンドを実際に走らせ、**公式ドキュメントの記述と実測が食い違うことを発見した**
  （「固定しなければ SDK 54 になる」と書かれているが、実測では SDK 57 ができた）。
  この食い違いにより「固定しない」案と「listings だけ固定」案が実測で消え、
  版の固定が消去法ではなく証拠で1つに絞られた。B20 は 05 の users テーブルへ反映済み。
  **B17 のうち実機での確認だけが未取得**（実機を使わない指示のためシミュレータで代替）。
