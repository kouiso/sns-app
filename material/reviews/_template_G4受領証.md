# G4 受領証 — 章 `<章ID>`

この様式は `material/decisions/D15_G4受領証の様式と発注手順.md` が正本。
記入済みの受領証は `material/reviews/g4/<章ID>.md` に 1 章 1 ファイルで置き、
ラウンドを下へ追記する（ラウンドごとにファイルを分けない）。
テンプレート自身は `_` 始まりのファイル名で `g4/` の外に置き、検査対象から外す。

**空欄・`<...>`・「未記入」を残したまま PASS を書かない。**
記入されていない欄がある受領証は、G4 未実施として扱う（10:28 の前作#10 対策）。

---

## 台帳（章ごとに1つ。ラウンドを追記する）

| 項目 | 値 |
|---|---|
| 章ID | `<章ID>` |
| 章タイトル | <章分割表 `material/18_章分割表.md` の当該行から転記> |
| 教材本文 | `curriculum/<章ID>.md` |
| 引用元 | `listings/<章ID>/` |
| 最新ラウンド | <R1 / R2 / R3> |
| 現在の総合判定 | <PASS / FAIL / エスカレーション中> |

### ラウンド一覧（修正回数のカウンタ）

同じ「対象タグ」の行数 − 1 が修正回数。**修正は2回まで（＝同一タグで R3 まで）。**
R3 が FAIL なら局長エスカレーション（10:211-212）。
対象タグが変わったら（実装側欠陥の修正で `chapter/<章ID>-r<K>` が打たれたとき。
10:318-322）カウンタは新しいタグで R1 から数え直す。

| ラウンド | 対象タグ | 教材ドラフトのコミット | 実施日 | ファミリー | 総合判定 | 修正回数 |
|---|---|---|---|---|---|---|
| R1 | `chapter/<章ID>` | `<sha>` | <YYYY-MM-DD> | <指定/代替・名前> | <PASS/FAIL> | 0/2 |

---

# R<n>（<YYYY-MM-DD>）

## 1. 実施条件

| 項目 | 値 |
|---|---|
| 対象タグ | `chapter/<章ID>` または `chapter/<章ID>-r<K>` |
| 教材ドラフトのコミット | `<sha>` |
| 使ったAIファミリー | <Codex/GPT系 など。16 A6 が要求する記録> |
| 指定か代替か | <指定 / 代替（指定が使えなかった理由: ...）> |
| モデル名・版 | `<実際に応答したモデルの識別子>` |
| 実行経路 | <CLI / Web など。再現できる形で書く> |
| 生成側ファミリー | <Claude。A6 の「同一ファミリーでの自己レビュー禁止」を満たすことの確認> |

## 2. 渡した入力（渡していないものも記録する）

渡してよい入力（D15-5）。渡したものにチェックを入れ、渡していないものは理由を書く。

- [ ] 教材ドラフト本文（**embedmd 展開後**。読者が読む形）
- [ ] `listings/<章ID>/` の全ファイル
- [ ] 章分割表の当該行（完成する状態 / 前提とする前章成果）
- [ ] `12_文体・AI臭さ排除方針.md`（文体逸脱の判定基準）
- [ ] 前章までの章タイトル一覧（順序の文脈）
- [ ] 前ラウンドの指摘と処置内容（R2 以降のみ）

渡していないことの確認（渡したら G4 は無効。D15-5）:

- [ ] `dev-logs/<章ID>.md`（開発ログ）を渡していない
- [ ] 設計書（`material/00`〜`16`。12 を除く）を渡していない
- [ ] 生成側のセッション文脈・生成プロンプト・推敲途中版を渡していない
- [ ] 対照版（手順書調の比較用の版）を渡していない
- [ ] 他の章と束ねずに、この章だけを1回の発注で渡した

## 3. 観点別 PASS/FAIL（合否を数えるのはこの3つ）

全観点 PASS で合格（10:208-209 の4観点から退屈度を外した形。理由は D15-3）。

| 観点 | 判定 | 根拠（レビュアの原文または要約） |
|---|---|---|
| 技術正確性 | <PASS/FAIL> | |
| 完成品へのつながり | <PASS/FAIL> | |
| 文体逸脱 | <PASS/FAIL> | |

### 3.1 引用範囲の過不足（技術正確性の必須小項目）

G5 は引用範囲の内側しか見ないため、ここが唯一の受け皿（10:227-233）。
**「指摘なし」ではなく、下の3問に明示的に答える。**

| 設問 | 判定 | 内容 |
|---|---|---|
| 教材だけを読んで写した読者が、章末の状態に到達できるか | <到達できる / 到達できない> | |
| 引用に含まれていないのに動作に要る行があるか（`import`・設定・依存の追加など） | <無し / 有り> | <有りの場合はファイルと行> |
| `listings/<章ID>/` の中で、章のどの引用範囲にも覆われていないファイルがあるか | <無し / 有り> | |

2問目・3問目に「有り」がある場合、技術正確性は FAIL。

## 4. 指摘一覧

重大度は CRITICAL（読者が必ず失敗する）/ MAJOR / MINOR。
判断は受注側が書く。**反駁で FAIL を PASS に書き換えない**（D15-7）。

| # | 重大度 | 箇所（ファイル:行 または引用） | 指摘 | 判断（成立/反駁） | 処置 | 処置コミット |
|---|---|---|---|---|---|---|
| 1 | | | | | | |

反駁した指摘がある場合、反駁の根拠（実測・公式資料・ファイル行）をここに書く:

- <#N の反駁根拠>

## 5. 退屈だった箇所（**合否に数えない**）

退屈度の合否判定は G6 の比較評価に一本化されている（10:210, 10:298-317）。
ここはレビュアが指した箇所を落とさないための記録欄で、PASS/FAIL を付けない。
記入内容は、その章の G6 比較評価の設問2（退屈だった箇所）へ持ち越す。

- <指された箇所。無ければ「無し」>

## 6. 総合判定と次のアクション

| 項目 | 値 |
|---|---|
| 総合判定 | <PASS（3観点すべて PASS） / FAIL> |
| このラウンド後の修正回数 | <n>/2 |
| 次のアクション | <G5 へ進む / 修正して R<n+1> を発注 / 局長エスカレーション> |

## 7. 局長エスカレーション（R3 も FAIL のときだけ記入）

| 項目 | 値 |
|---|---|
| エスカレーション日 | <YYYY-MM-DD> |
| 残っている FAIL 観点 | |
| 渡した資料 | <受領証のパス・章のパス・3ラウンドの差分> |
| 局長の裁定 | <未回答 / 内容> |
| 裁定後の処置 | |

裁定が出るまで、この章の教材PRはマージしない（10:207 の「自動保存せずエスカレーション」）。
